# SYNC.md — keeping eve-trader-local (offline) and eve-trader (online) aligned

This repo and `eve-trader` (https://github.com/Pappmichel/eve-trader) are
two separate codebases, not a fork/branch pair — they deliberately diverge
at the storage/config layer (SQLite + a plain dataclass here vs. Postgres +
RLS + `ConfigProxy` there, see this repo's own README). There is no tooling
that syncs them automatically. This file is the manually-maintained
contract for *what* needs porting by hand when it changes, and *what never
should be*. The two copies of this file (one per repo) should stay
identical in content — update both together.

**In scope for syncing:** general business-logic/behavior — pricing
formulas, ESI/Goonmetrics parsing and math, classification rules, the OAuth
flow itself. **Out of scope, by design:** anything tenant-specific,
user-specific settings/data, or access-control/multi-tenant machinery — see
"Never sync" below. When in doubt: would a single local user's own settings
or a specific tenant's data ever appear in the diff? If yes, it's not a sync
candidate.

## Already ported

### Trading (plus the shared SDE/ESI/config foundation)

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/auth.py` | `eve_trader/auth.py` | EVE SSO authorization-code+PKCE flow: state/PKCE generation, loopback callback handling, refresh logic | Ported 2026-09-02. Storage calls adapted here (no tenant scoping), locking removed (single-process, no shared thread pool to guard) — port algorithm changes only, not the storage glue. |
| `eve_trader_local/esi_client.py` | `eve_trader/esi_client.py` | The whole generic ESI HTTP layer: `OrderStats`/`_summarize_orders`/`_percentile`, the retry-with-backoff core (`_get_response`/`_post_response`, `_retry_after_seconds`), ESI's `X-Esi-Error-Limit-*` budget tracking, `_get_all_pages`' page-1-then-concurrent-rest pagination, the class-level price/cost-index and short-TTL order-book caches, and every endpoint wrapper (character/corp orders, assets, wallet, industry jobs, blueprints, contracts, skills, structures, name/system resolution, market history, adjusted prices, cost indices) | Ported 2026-09-02, near-verbatim. `structure_order_stats_bulk_or_goonmetrics` followed 2026-09-02 with the `goonmetrics_client.py` port (see the row below) — it was held back only because it's the one method depending on that client. Adapted: the parent's `storage.with_current_tenant` wrapper around every `ThreadPoolExecutor.submit` is dropped (it exists purely because a worker thread doesn't inherit the tenant contextvar; there is no ambient tenant here), and `ESIError` subclasses this repo's `ActionError` (the parent keeps them separate only to dodge a `config.py`↔`actions.py` circular import — see `errors.py`). `resolve_effective_volume`/`_bulk` came over with two new SQLite-side helpers they need (`storage.get_type_category`, plus a `type_packaged_volume` cache table and its getter/setter) — that table is an ESI-only per-type constant, so it deliberately survives an SDE refresh. |
| `eve_trader_local/goonmetrics_client.py` | `eve_trader/goonmetrics_client.py` | The current-price fetch shape (`appraise.gnf.lt/market/{slug}/prices.json`, its `CurrentPrice` parsing and retry-with-backoff) and the caching *pattern*: module-level cache dicts keyed by market, with a per-market lock rather than one shared lock, so a multi-second Jita fetch never blocks a concurrent home-market fetch that shares no cache entry with it. Plus `ESIClient.structure_order_stats_bulk_or_goonmetrics` (in `esi_client.py`, where the parent keeps it too) — the failsafe that falls back to a Goonmetrics snapshot when no seller is logged in or the real structure order book fails, returning `(stats, used_fallback)` with volumes zeroed. Plus the region price-history half: `price_history`/`price_history_chunked`/`HistoryPoint`/`_parse_history_xml` (`goonmetrics.apps.gnf.lt/api/price_history/`), its silent per-type ESI history fallback for when that no-SLA API is down — where `movement` passes ESI's `volume` (a unit count) straight through, never multiplied by average price, the parent's own fix for silently mixing unit counts and ISK values in one field — and the chunked fetch's per-chunk isolation so one bad chunk can't lose a whole multi-minute search. | Ported 2026-09-02 (current prices), completed 2026-09-02 with the price-history half. Both endpoints are now here, and the module docstring spells out which question each answers — a region history average ("was this historically worth importing", asked against `reference_region_id`) is not interchangeable with a live current quote. Added here: `TradingConfig.goonmetrics_appraise_base` and `goonmetrics_history_base` (the parent hardcodes the former as a module constant `APPRAISE_BASE`; this repo already has an `esi_base` config precedent, so both are fields) plus `chunk_size` (with a `_FIELD_RANGES` entry). |
| `eve_trader_local/candidate_discovery.py`, `eve_trader_local/models.py` | `eve_trader/candidate_discovery.py`, `eve_trader/models.py` | The whole discovery pass: `is_wanted_market_path`'s exclusion-prefix-only filtering (no keyword allowlist, no per-item m3 cap — an item is only ever dropped for failing on real profitability/volume later), `guess_category`'s real-SDE-category classification (with the string/volume heuristic kept strictly as the live-ESI-walk fallback, and the `IMPLANT_CATEGORY_ID`/`BOOSTER_GROUP_ID` split that keeps drugs from being labelled implants), `_market_group_path`, the SDE-cache-first/live-ESI-walk-fallback `build_candidate_universe`, and `build_focused_candidate_universe` (a deliberate pass-through). Plus the `Candidate` dataclass. | Ported 2026-09-02, near-verbatim — including the parent's issue #73 (capital-sized modules need their *packaged* volume, not the SDE flight volume) and issue #96 (resolve it in one bulk concurrent pass, never per-type inside the loop) context, which are real bugs, not stylistic notes. Added here to support it: `TradingConfig.excluded_path_prefixes`, `storage.load_sde_market_groups`/`load_sde_types_with_market_group`/`load_sde_category_names`/`load_sde_type_groups` (part of the SDE read-helper set the `sde.py` row deliberately deferred), and the `candidate_universe`/`focused_candidates` tables with `save_candidate_universe`/`load_candidate_universe`/`get_candidate_universe_built_at` — persistence is the *caller's* job in the parent too (`actions.do_build_universe`), so the module itself stays free of storage writes. `load_candidate_universe` returns `Candidate` objects where the parent's `read_table` returns a pandas DataFrame (no pandas here, and every caller wants the dataclass). Only `Candidate` came from the parent's `models.py` in this row; its other dataclasses arrive with `shortlist.py`/`trade_reconciliation.py` (`NewCandidateResult` arrived with `history_backtest.py`, see the row below). |
| `eve_trader_local/history_backtest.py`, `eve_trader_local/models.py` | `eve_trader/history_backtest.py`, `eve_trader/models.py` | The whole backtest/scoring pass: `_index_history`, `_latest_margin`, `_score_candidate` (the landed/net_sell/margin formula, `score = avg_profit_m3 x log(1+avg_move) x hit_rate`, and the five-part `add` gate), `compute_margin_trends` (3-day vs 30-day rolling-margin momentum, with `MIN_TREND_HISTORY_DAYS` and the `MIN_BASELINE_MARGIN_MAGNITUDE` near-breakeven guard), `select_candidate_window`'s rotating coverage, and `find_new_import_candidates`/`_safe` with their per-batch isolation and incremental sinks. Plus the `NewCandidateResult` dataclass. | Ported 2026-09-02, near-verbatim apart from `compute_margin_trends`. **Pandas deliberately not added**: the parent's version takes a DataFrame, but everything it does with it is an inner join on `(type_id, date)`, a per-type grouping and two tail-window means — no vectorised math, no rolling/resampling, and at this size (a few thousand rows per candidate search) plain dicts/lists are just as fast. It takes an `Iterable[HistoryPoint]` here instead, which is also the exact shape `find_new_import_candidates`' `history_sink` already emits, so no DataFrame-shaped storage read has to exist for it. Same dependency-light call the `candidate_discovery.py` row already made. Added here to support it: `TradingConfig.safe_mode_max_ids`/`min_margin_threshold`/`min_hit_rate`/`min_avg_movement` (each with a `_FIELD_RANGES` entry). Deliberately left out: any storage coupling at all — persistence (the parent's `storage.save_goonmetrics_history`/`save_new_candidates`/`candidate_search_cursor`) is the caller's job, same precedent as `candidate_discovery.py`, and the callbacks/offset that let a caller do it are ported; those three SQLite helpers arrive with the future `do_find_new_candidates` equivalent, which this repo has no actions layer for yet. |
| `eve_trader_local/shortlist.py`, `eve_trader_local/models.py` | `eve_trader/shortlist.py`, `eve_trader/models.py` | The whole per-row evaluation: `_decision`'s precedence ladder (Inactive → Missing ID → No market data → Skip → Already ordered/Import, with "No market data" and "Skip" deliberately kept as two labels), `evaluate_shortlist_item`'s landed-cost/net-sell/margin/profit-per-m3 formula (priced off the *ask*, not the bid, with `jita_buy_broker_fee` still applied — deliberately conservative, not an oversight) including its "an inactive item is still fully priced" rule, `evaluate_shortlist`'s one-pass-over-pre-fetched-stats shape, `average_market_daily_volume`, `summary_counts`, `top_imports_by_daily_profit` and `audit_shortlist`. Plus the `ShortlistItem`/`ShortlistRow` dataclasses. | Ported 2026-09-02, near-verbatim. The two historical bugs this file's shape exists for came over intact, in the code rather than only in this table: **Profit / Day is `profit_per_unit x avg_daily_volume`** (real market-wide traded quantity from Goonmetrics region history for `reference_region_id`, via `average_market_daily_volume`) — never `sell_volume`/order-book depth (the parent's issue #51: a single parked sell order inflated the figure from listed quantity alone) and never the trader's own realized sales (issue #100: #51's overcorrection left Profit/Day empty for every not-yet-sold-by-me candidate, i.e. most of what a shortlist is for). An item Goonmetrics has no history for is excluded from Top Imports rather than estimated from something else. `sell_volume` is still carried as "Listed Qty" and used only as the "is anything listed at all" gate in `_decision`. **Storage-ownership decision: shortlist membership is persisted, but the persistence stays in `storage.py`, not in `shortlist.py`** — the parent's `shortlist.py` imports no storage at all (every `storage.*` call is in its `actions.py`), and this repo already keeps every table read/write in one module; `shortlist.py` therefore stays a pure, network-free, storage-free computation over values handed to it, same as `candidate_discovery.py`/`history_backtest.py`. Added here to support it: the `shortlist`/`shortlist_snapshot` tables with `upsert_shortlist`/`load_shortlist`/`deactivate_shortlist_items`/`activate_shortlist_items`/`update_shortlist_meta_levels`/`save_shortlist_snapshot`/`latest_shortlist_snapshot` (the last returns `ShortlistRow` objects where the parent's `latest_snapshot` returns a DataFrame — no pandas here), and `TradingConfig.min_profit_threshold` (with a `_FIELD_RANGES` entry bounded at 0 only, never at 1 — a target margin above 100% is a legitimate setting, not a typo). Deliberately left out: `shortlist_skip_streak` and `skip_grace_period_days`/`max_active_shortlist_items` — those belong to the parent's `do_refresh_and_prune_candidates` pruning policy, which lives in `actions.py` and isn't ported yet; they arrive with it. |
| `eve_trader_local/own_orders.py`, `eve_trader_local/models.py` | `eve_trader/own_orders.py`, `eve_trader/models.py` | All four live order-management checks: `fetch_own_sell_orders` (one pass over the character's orders producing `{type_id: volume_remain}` for open sells at `structure_id`, instead of one filtered query per shortlist row), `check_undercut`/`_pooled` (which of the seller's own listings a competitor has beaten on price), `fetch_seller_stock_without_order`/`_pooled` (shortlist stock physically at the structure with no sell order on it at all), and `fetch_buyer_already_covered` (items already on a buy order or already in inventory at Jita/the structure, so they needn't be imported again). Plus the `UnlistedStockRow`/`UndercutRow` dataclasses. | Ported 2026-09-02, near-verbatim. Two real constraints came over in the code, not just in this table: **"which orders are mine" can only be decided by `order_id`** — ESI's structure order book carries no owning-character field at all (confirmed against the endpoint's response schema), so type_id/price matching would false-positive on a coincidentally identical competitor price; and **the own-order exclusion is pooled across every registered seller character** (the parent's issue #46 — several sellers share one structure's order slots, and a cheaper order from another of *your own* characters must never read as "undercut"). Same pooling applies to unlisted stock: quantities add up and any seller's order covers any seller's stock, since the structure hangar is shared. The parent's deliberate scope limits are kept as-is: only shortlist items are considered, only a *complete* absence of a sell order is flagged (a partially listed item is not), and only assets sitting directly in the structure's own `location_id` are seen (anything nested in a container or parked ship carries that container's item_id — the same one-level-flat limitation `fetch_own_sell_orders` has). **Storage-ownership decision: this module calls storage itself, following `trade_reconciliation.py`'s precedent rather than `shortlist.py`'s** — because the parent's own `own_orders.py` imports `storage` and calls it directly (`get_station_ids_in_system`, for the buyer-side "is this asset in Jita" check), so this file mirrors the parent's actual structure; the SQL stays in `storage.py`, and nothing here writes anything. Added here to support it: `storage.get_station_ids_in_system` (without the parent's `lru_cache`, same call this repo already made for its other SDE reads) and `esi_client.NON_STOCK_LOCATION_FLAGS`. That constant lives in `storage.py` in the parent because its persisted asset tables filter on it; **here it is in `esi_client.py` instead** — it describes ESI's asset payload, and this repo has no persisted asset table for it to sit beside. Deliberately left out: item-name resolution, the shortlist-item-id set, the margin/`sell_volume` enrichment and the "log in again for esi-assets.read_assets.v1" error message — all of those live in the parent's `actions.do_check_seller_unlisted_stock`/`do_check_undercut`, not in `own_orders.py`, and arrive with this repo's future action layer. `UnlistedStockRow`/`UndercutRow` are ported with it (the parent builds them in `actions.py` from these functions' dicts; they are the documented output shape of this module either way), so the display layer has them the moment that layer exists. |
| `eve_trader_local/sde.py` | `eve_trader/production/sde.py` | Fuzzwork CSV fetch/parse: the file list, `_fetch_csv`'s retry-with-backoff, `_dump_etag`'s ETag freshness check, every row-shaping rule (the `_RELEVANT_ACTIVITIES` filter, the slot-defining dogma effect IDs, `metaGroupID`/`portionSize` handling) | Ported 2026-09-02, near-verbatim. **Data layer only.** Deliberately left out: `ProductionConfig.fuzzwork_csv_base` (a module constant here — no Production config exists yet), and most of the parent's `storage.py` SDE *read* helpers (`load_sde_*`, `get_type_category`, `get_blueprint_*`, `find_invention_recipe_candidates_*`, `get_type_slot`, `get_type_materials`, …) — those exist to serve Production/Doctrine/Refining business logic that isn't ported here yet; port each one when the feature that needs it arrives. Only `sde_row_counts`, `get_sde_type` and `search_sde_types` came over, to make `refresh-sde`/`sde-status` verifiable by a human. The parent's `lru_cache` memoisation on those reads was dropped with them (nothing here calls them in a hot loop, and a cache would need invalidation on every refresh). **Amended 2026-09-02**: the four SDE activity IDs are no longer inlined here — `production/constants.py` is ported now and owns them, same import direction as the parent (see the Production row below). |
| `eve_trader_local/trade_reconciliation.py`, `eve_trader_local/models.py` | `eve_trader/trade_reconciliation.py`, `eve_trader/models.py` | The whole realized-P&L pass: `fetch_recent_transactions`' `from_id` cursor paging (a single un-paginated call only ever sees ESI's most recent 2500 transactions, which silently dropped older-but-still-in-window trades), `fetch_recent_journal_entries`' best-effort `market_transaction` lookup, `reconcile_realized_trades`' per-type FIFO match (pooled across every buyer/seller character, not paired 1:1) with both location filters and the `buy_date <= sell_date` rule, the landed-cost/net-sell formula including the wallet-journal real-tax path and its `_ASSUMED_TAX_RATE_IN_DEFAULT_HAIRCUT` back-out, the on-demand ESI type backfill, `summarize_realized` and `average_daily_sold_by_type`. Plus the `RealizedTrade` dataclass. | Ported 2026-09-02, near-verbatim. The two matching bugs the parent's shape exists for came over in the code, not just in this table: **a buy dated after its sell is never a cost basis** (FIFO alone only orders buys chronologically; in the parent 20% of matched rows, and 21.8% of reported profit, had `buy_date > sell_date` — the matcher breaks and drops the sell's unmatched remainder rather than substituting a later buy), and **the buy side is region-filtered too** (it had no location filter at all, so any wallet transaction the buyer made anywhere could be paired against an unrelated structure sale). `_BUY_LOOKBACK_MULTIPLIER` (buys fetched over 3x the sell window) came with them, for the same reason. `average_daily_sold_by_type` is deliberately kept even though the shortlist no longer uses it: it answers the narrower "how much have *I* sold of this" question, not the market-wide Profit/Day figure that `shortlist.average_market_daily_volume` now answers (issue #100 superseded issue #51's realized-sales version *there*, it did not delete this). **Storage-ownership decision: this module calls storage itself, unlike `candidate_discovery.py`/`history_backtest.py`/`shortlist.py`** — the parent's own `trade_reconciliation.py` imports `storage` and calls it directly in two places (`get_station_ids_in_region` for the buyer-side filter, and a `realized_trades` read in `average_daily_sold_by_type`), where the parent's `shortlist.py` imports no storage at all. So this file follows the parent's actual structure rather than the shortlist precedent: the calls live here, the SQL still lives in `storage.py` (this repo's one persistence module), and *writing* a run's results stays the caller's job exactly as in the parent's `actions.do_reconcile_trades`. Added here to support it: the `realized_trades` table (replaced wholesale per run, not appended — a naked INSERT had bloated the parent's to ~19.8k rows over 13 runs) with `save_realized_trades`/`latest_realized_trades` (the latter returns `RealizedTrade` objects where the parent's `read_table` returns a DataFrame — no pandas here), and `storage.get_station_ids_in_region`, without the parent's `lru_cache` (nothing calls it in a hot loop, and a cache would need invalidating on every SDE refresh — the same call this repo already made for its other SDE reads). No config fields were needed: `lookback_days`/`import_cost_per_m3`/`jita_buy_broker_fee`/`structure_sell_haircut`/`jita_region_id`/`structure_id` all already exist. |

### Cross-cutting (Portfolio, logging)

Two small top-level parent modules that aren't part of any one tool.

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/portfolio.py` | `eve_trader/portfolio.py` | The whole combined-overview computation: `portfolio_overview`'s Trading-realized-profit-plus-Production-stock-value summary, its weighted-average-margin formula (shared with `trade_reconciliation.summarize_realized`), and the day-to-day realized-profit population-stdev volatility signal (`None` below 2 days of data — a single day has no meaningful spread). Each half degrades independently to zero/None when that tool has no data yet, exactly matching the parent's own "additive, never a replacement for either tool's own view" framing. | Ported 2026-09-03, near-verbatim. Adapted, not ported: the parent reads `storage.read_table("realized_trades")` (a pandas DataFrame) and does the day-grouping/stdev via `.groupby`/`.std(ddof=0)`; this repo has no pandas, so it reads `storage.latest_realized_trades()` (already-ported, returns `RealizedTrade` objects) and does the same grouping/`ddof=0` population-stdev by hand in a small `_population_stdev` helper — same math, same result, no DataFrame dependency, same precedent every other Trading module's storage-return-shape adaptation already set. `stock_value`/`PRODUCTION_CONFIG` are imported lazily inside the function, matching the parent's own lazy import there (avoids paying for the Production import path at all when no stock targets are configured). CLI: `portfolio-overview`, via a direct call to `portfolio_overview()` — no `do_*` action wrapper, since (like `portfolio.py` in the parent) it's a pure read with no user input to validate and nothing an `ActionError` boundary would ever need to catch. Tests: `tests/test_portfolio.py` (data in both tools, Trading-only, Production-only, no data in either, and the single-day-has-no-volatility case). |
| `eve_trader_local/logging_setup.py` | `eve_trader/logging_setup.py` | The *concept* only (a `configure_logging()` idempotent rotating-file-handler setup for the package's own logger namespace) — not the console-handler half, and not the reasoning it's scoped around. | **Deliberately simplified, not ported verbatim** — judged worth having in a smaller form, not skipped outright. The parent's version exists because `scheduler.py` runs background jobs for hours with nobody watching a terminal, so it needs both a console handler (for whoever *is* watching) and a file handler (for whoever isn't). This repo has no long-running process at all — `cli.py`'s own docstring: "commands run and exit" — so a console handler would just duplicate what every command's own `print()` already puts on screen in the same process. What's still real: several already-ported modules (`candidate_discovery.py`, `history_backtest.py`, `goonmetrics_client.py`, `actions.py`, `refining/actions.py`) call `logger.exception`/`logger.warning` on deliberately-swallowed best-effort paths, which today only reach Python's unformatted "handler of last resort" and vanish once the terminal closes — a real gap for diagnosing a `pipeline`/`sync-esi` run after the fact. So this repo ports a file-handler-only `configure_logging()` (2MBx2 backups, scoped to the `"eve_trader_local"` namespace, same non-propagating-past-root reasoning), wired into `cli.py`'s `main()`, and nothing else. Added a `reset_for_tests()` this repo needs that the parent doesn't (the parent's server process only ever calls `configure_logging()` once for real; this repo's test suite calls `main()`-adjacent code across many independent tests in one process and needs to clear the idempotency guard + attached handler between them so each test's own tmp data dir is actually honored, not silently shadowed by whichever test's data dir got there first). Tests: `tests/test_logging_setup.py` (a real log record lands in the configured data dir's file, and a second `configure_logging()` call doesn't attach a duplicate handler). |

### Production

A separate tool from Trading in the parent repo too — the two share only the
SDE/ESI/config foundation. Production's backend is now fully ported:
classification, the constants table, the material-acquisition pricing layer,
the invention math, the buy-vs-build cost/margin core (ME/TE stacking, job
costs, recursive BOM expansion), `ProductionConfig`, build-candidate
discovery, the stock-aware planner (`plan_production`, with a genuinely new
local concept - `storage.stock_targets` - to feed it, plus its invention-needs
list and a real manual-stock ledger), the readiness-focused
`plan_asset_optimized`, `market_status`/`stock_value`, Special Orders
(`plan_special_order`, ad-hoc one-off build orders sharing that same BOM/
pricing core), the Margin page's ship browser (`discover_ship_margins`), and
the invention-location slice of the logistics helpers (`invention_logistics`/
`t1_bpc_invention_needs`), and now the multi-structure Logistik-tab helpers
(`logistics_status`/`distribution_recommendations`, GitHub issue #4) are all
ported - see the `engine.py` row below for how the earlier "out of scope for
a single-structure install" call got revisited and reversed.

**Amended 2026-09-03**: `sde-status`'s freshness check (previously only the Fuzzwork-newer-SDE half) now also compares `storage.get_sde_refresh_state()`'s timestamp against `storage.get_candidate_universe_built_at()` (both already existed) and warns when Trading's cached candidate universe now predates the last SDE refresh - ported from the parent's `do_check_sde_freshness`, including its whole-second-prefix-only timestamp comparison (the two timestamps come from different `isoformat()` calls with different trailing precision). Kept inline in `cli.py`'s existing `cmd_sde_status` rather than a new `do_*` action, matching that command's own pre-existing shape (it already called `sde.check_for_newer_sde()` directly, with no `do_*` wrapper of its own).

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/production/constants.py` | `eve_trader/production/constants.py` | The whole file: SDE activity IDs, `ACTIVITY_MODS`, `STRUCTURE_TYPES`/`RIG_TIERS` and `structure_rig_multiplier`/`rig_security_multiplier` (including `_rounded_security`'s CCP 1-decimal rule), `DECRYPTORS`, `SCC_SURCHARGE_RATE`, the meta-group IDs, `COMPONENT_GROUP_IDS`/job-category/ship-size group sets, and the job-slot skill IDs with `job_slots_from_skills`. All of it is fixed real-EVE game data with no config or storage dependency, so it is pure sync material — when CCP changes a bonus, both repos change identically. | Ported 2026-09-02, near-verbatim (in full, not just the parts `classify_activity` needs — the file is one flat table of constants and splitting it would only make the next Production port re-derive the split). Comments referencing not-yet-ported consumers (`_activity_mods`, the ship-margin scan, `invention.py`'s `best_recipe_and_decryptor`) were rephrased to name the parent, not a local function that doesn't exist — the *reasoning* is preserved verbatim, only the cross-reference moved. **Activity-ID duplication decision:** `sde.py` used to inline `ACTIVITY_MANUFACTURING`/`REACTION`/`COPYING`/`INVENTION` with a comment saying it did so only because no Production module existed to own them. That condition is now gone, so they have exactly one definition here and `sde.py` imports them — the same direction as the parent, where `production/sde.py` imports them from `production/constants.py`. No cycle and no layering inversion: this module imports nothing at all (not even `storage`), so it is a leaf the data layer can depend on. |
| `eve_trader_local/production/engine.py` | `eve_trader/production/engine.py` | `classify_activity`: the Reaction/Tech II/meta-group/Tech I decision ladder and the `meta_group_labels` mapping (metaGroupID 4/5/3/6 → Faction/Officer/Storyline/Deadspace). Plus (2026-09-02) the whole buy-vs-build cost core: `_material_qty`'s per-batch EVE rounding rule, the structure/rig profile split (`_is_component`/`job_category`/`_structure_profile`/`_structure_rig`/`_security_multiplier_for`), `_job_cost_rate`'s additive index×structure-bonus + facility-tax + SCC-surcharge formula with its three manual overrides, both ME/TE-stacking layers (`_activity_mods`, `_tech_ii_mods`' net-cost decryptor selection), `_unit_cost`/`unit_cost_detail`'s recursive best-of-buy-or-build with the EIV-priced job fee, `_buy_or_build_decision`, `_sell_margin`/`margin_home`/`margin_jita`, and the two price-agnostic BOM walks (`structural_material_closure`, `build_material_tree`). | Ported 2026-09-02, verbatim apart from the docstring's cross-references. **This is the one file in the Production port whose reasoning must never be simplified away:** "Tech II" is decided *solely* by a real SDE invention-recipe lookup, never by `metaLevel`. The parent's `metaLevel >= 2` heuristic mislabelled 854 of 4208 scanned manufacturable items as Tech II — every Faction/Pirate hull (Machariel/Nestor are metaLevel 8), Officer/Deadspace module and faction booster — and each then got priced off the flat Tech II ME/TE baseline instead of the Tech I one. It also *missed* genuinely invented Tech III subsystems (a Loki Core is metaLevel 1). Both halves are covered by explicit regression tests (`tests/test_production_engine.py`). No `ProductionConfig` was created: classification is a pure function of the SDE cache with zero config dependency — the config class arrives with the first pricing/buy-vs-build port that actually needs a field. Added here to support it: `storage.get_blueprint_for_product` and `storage.find_invention_recipe_candidates_by_product_type_id`, ported from the parent's `storage.py` (both read tables `sde.py` already populates; the parent's `lru_cache` dropped, same call this repo made for every other SDE read). The parent's `published = 1` filter on the blueprint lookup came with it — a leftover unpublished CCP test blueprint would otherwise make an unbuildable item look buildable — and has its own test. **Amended 2026-09-02** with the cost/margin port above, near-verbatim, including every real-EVE rule the parent's comments exist for: material quantities are rounded up once per *batch* and floored at `runs` (a base_qty=1 material is never ME-reduced at all), the facility tax and SCC surcharge are *additive* terms and not folded into the cost-index multiplication (folding them in understated every job fee by ≥4.25% of EIV — a confirmed parent bug), the job fee is `EIV × rate` off ESI *adjusted* prices at ME-0 quantities while material cost uses ME-reduced quantities at real market prices, and rig bonuses scale with system security off two different tables (Engineering Complex vs. reactor). **Amended again 2026-09-02** with the owned-BPO ME/TE preference, which is **no longer a divergence**: `_owned_bpo_mods` and `_activity_mods`' optional `blueprint_id` argument are both restored, now that `production/esi_sync.py` populates `character_blueprints`/`corp_blueprints` and `storage.get_owned_bpo_best_me_te` can answer from real data. Tech I now prices off your actual researched BPO — best ME and TE taken *independently* across every owned Original, copies excluded — and falls back to the flat perfect-research baseline only when you don't own that blueprint or haven't synced yet; Reaction and the four meta-group activities still never consult it (a real EVE mechanic, not a fallback). Covered by `tests/test_production_engine.py`, including one test asserting a changed *build cost*, not just a changed multiplier. Remaining divergences, all deliberate: **the bought-blueprint-copy cost term is left out** (`storage.get_manual_blueprint_copy_cost_per_run`, a manual user-entered table with no entry point here); **`_job_cost_rate`'s per-job-category cost-index refinement is left out** (parent issue #12 — it reads the Logistik tab's per-category structure assignments, a web-UI concept with no local table, so the `CostIndices` map here carries only the flat component/manufacturing split and the `category:` key never appears); `component_system_name`/`manufacturing_system_name` are skipped with it (the parent resolves names to ids in a web-only Settings action; here the id is configured directly). Two small structural changes: `_unit_cost`/`unit_cost_detail` share one `_material_cost_and_eiv` helper where the parent deliberately duplicates the formula (the parent's reason was to leave `_unit_cost`'s existing recursion and tests untouched, which doesn't apply to a fresh port — and duplicating a formula whose whole point is that both callers price a build *identically* invites exactly the drift the parent's own comment warns about), and `_structural_material_closure` is public here (`structural_material_closure`) since its caller will live outside this module. `_build_margin` (the thin dispatcher choosing between `margin_home`/`margin_jita` by whether a stock target has a Jita target) is deferred with the planner that owns stock targets; both margin functions themselves are ported. Added here to support it: `storage.get_system_security` (reads the `sde_solar_systems` rows `sde.py` already populates; the parent's `lru_cache` dropped, same call this repo made for every other SDE read — the parent cached it for its planner's per-BOM-node hot loop, which doesn't exist here yet). **Amended 2026-09-02** with `discover_build_candidates`/`_scan_build_candidates` and `MAX_PLAUSIBLE_BUILD_MARGIN` — this repo's Production equivalent of Trading's `find-candidates`: scans every published, market-listed SDE item with a real recipe (`storage.load_sde_types_with_market_group`, already ported) and gates on `cfg.min_margin` (via `margin_home` — home-structure only, never Jita, matching the parent's confirmed C-J-centric business model), then ranks the survivors by `potential_daily_profit = daily_movement × margin × build_cost`, gated again by `cfg.min_daily_profit` — a batched `goonmetrics_client.price_history_chunked` lookup against `TradingConfig.reference_region_id`, same theoretical-ceiling framing as Trading's own Profit/Day (see CLAUDE.md's "Theoretical ceiling" section): a tiny-volume, huge-per-unit item can still show an enormous number, and that's correct, not a bug to cap. **Real, deliberate simplification from the parent's version, not an oversight:** there is no `existing_target_ids` exclusion (this repo has no stock-target table yet to exclude against — see the planner row in "Candidates" below) and `selected_decryptors` is always `{}` (no manual-decryptor-override table exists here either), so every Tech II/III candidate prices off whichever decryptor is cheapest in net terms, never a manual pick. **Amended 2026-09-06 (Windows local-performance audit)**: `discover_build_candidates` is now cached too (`_DISCOVER_CACHE_TTL`/`_discover_cache`/`invalidate_discover_cache`, single global entry — no per-tenant keying needed here), and the SDE point-lookups its scan/BOM-traversal hit repeatedly (`storage.get_sde_type`/`get_type_materials`/`get_type_category`/`get_blueprint_for_product`/`get_blueprint_materials`/`find_invention_recipe_candidates_by_product_type_id`) now carry the same `lru_cache` the parent always had (previously dropped here with "no such caller exists here yet" reasoning that stopped being true once this scan/BOM-walk was ported) — both were the root cause of the local Windows install's slow planner/build-list runs (each of thousands of ungated SDE lookups was opening its own fresh SQLite connection). Invalidated the same way the parent does: a Settings save (`do_update_settings`) and an SDE refresh (`replace_sde_data`, called by `sde.refresh_sde` from both the CLI and the GUI's SDE refresh dialog). `discover_ship_margins`/`_scan_ship_margins` (the Margin page's separate, ungated ship browser) is judged a genuinely separate function from `discover_build_candidates` and is left for a future port — see the "Candidates" row below. Added here to support it: `production/models.py`'s `BuildCandidate` dataclass (the parent's own return shape) and `ProductionConfig.min_margin`/`min_daily_profit` (see the config.py row below). CLI: `discover-build-candidates` (`--top-n`), via `production/actions.py`'s `do_discover_build_candidates` (same `ActionError`-on-empty-SDE-cache guard as the parent's `do_discover_build_candidates`). Tests: `tests/test_production_discovery.py`, against a small synthetic SDE with `pricing.home_prices`/`jita_prices`/`ESIClient` monkeypatched and a fake Goonmetrics client — covers the margin gate, the `min_daily_profit` gate (including the "no history → 0.0 movement, never estimated" case), the `buy == sell` synthetic-quote skip, `top_n` slicing, and that `potential_daily_profit` is computed as the same market-wide-turnover formula as Trading's Profit/Day, never scoped to one builder's own sales. **Amended 2026-09-03** with the stock-aware planner (`plan_production`) and its supporting `_current_stock`/`_base_runs`/`_parent_base_runs_for_buffer`/`_expand_all`, now that `storage.stock_targets` (below) gives it something to plan for; `_build_margin` (the home-vs-Jita dispatcher, previously deferred) is restored alongside it, and `discover_build_candidates`'s `existing_target_ids` exclusion (previously "no stock-target table to exclude against") is now wired up. **Real, deliberate simplification from the parent, confirmed with the user rather than a gap**: a stock target here is one target quantity plus a `jita_target` bool (feeding `_build_margin`'s dispatch, nothing else) — not the parent's three-way backup/home-market/Jita-market split with its own live sell-order-listing nets (`_total_missing`, `market_status()`'s own targets); and there is still no manual-stock override table, so `_current_stock` is ESI-assets-plus-incoming-jobs only (no `manual_stock` term). `plan_asset_optimized` (the parent's second, readiness-focused Bauliste — splits "physically on hand right now" from "current_stock incl. incoming jobs" to show which jobs are startable *today* vs. blocked on an upstream shortfall) is judged a genuinely separate function, not a variant of `plan_production`, and is left unported — it needs its own on-hand-only stock ledger this repo has no manual-stock table to make meaningfully different from `plan_production`'s own numbers yet; same for the parent's invention-needs list (`storage.available_blueprint_copies`/`get_blueprint_time`, neither ported) and `market_status`/`stock_value` (thin display reads over the same three-target model this repo doesn't have). Added here to support it: `storage.stock_targets` (`type_id, type_name, quantity, jita_target`) with `upsert_stock_target`/`delete_stock_target`/`load_stock_targets`, and `production/models.py`'s `InventoryRow`/`BuyListEntry`/`BuildJobEntry` dataclasses (subsets of the parent's own — no `job_time_seconds`/invention columns, since neither `get_blueprint_time` nor the invention-needs machinery is ported). `ProductionConfig.component_overbuild` (0.7 default, matching the parent) — the one field the planner's overbuild-buffer math needs that no prior port had added yet. CLI: `set-stock-target <item> <qty> [--jita]` / `remove-stock-target <item>` / `list-stock-targets` (name-or-type_id resolution via `storage.search_sde_types`, same exact-match shape `discover_build_candidates`-adjacent lookups already use) and `plan-production`, via `production/actions.py`'s `do_add_stock_target`/`do_remove_stock_target`/`do_list_stock_targets`/`do_plan_production`. Tests: `tests/test_production_planner.py` (current-stock netting against ESI assets + incoming industry jobs, multi-stock-target BOM aggregation sharing a component, `_build_margin`'s home-vs-Jita dispatch) and `tests/test_storage.py`/`tests/test_production_actions_stock.py` (stock-target CRUD round-trip and the `do_*` name/type_id resolution). **Amended 2026-09-03: the rest of the parent's planner pairing is now ported too**, closing out the last "Candidates" row for this file. First, the manual-stock question was actually investigated rather than assumed (per the parent's real code, `storage.py`'s own `manual_stock` table/`upsert_manual_stock`/`load_manual_stock`): it genuinely is a separate, user-maintained on-hand count layered on top of ESI-synced assets ("I physically counted N units and it doesn't match what ESI thinks"), not a simplification of ESI assets that could've been produced by just calling `esi_stock_at_location` without `esi_incoming_industry_qty` - so it's ported as a real table (`storage.manual_stock`, CRUD via `upsert_manual_stock`/`delete_manual_stock`/`load_manual_stock`/`list_manual_stock`), following the exact same "simplest useful shape" precedent `stock_targets` itself set. `_current_stock` and the new `_stock_on_hand` (below) both fold it in. **`plan_asset_optimized`** is ported now that the manual-stock question is resolved: the readiness-focused second Bauliste, splitting "physically on hand right now" (`_stock_on_hand`, ported verbatim including the real bug its own docstring documents - an in-progress job's eventual output can't make a *different* job "Ready Now") from "current_stock incl. incoming jobs" (`_current_stock`, used for shortfall *sizing*) via two parallel stock-used ledgers and `_allocate_scarce_stock`'s smallest-claim-first allocation (also ported verbatim). **One real, deliberate simplification, not an oversight**: `AssetPlanJob.recommended_slots` and its whole `_free_slots_by_category`/`_allocate_slots_proportionally` machinery are left out entirely - that needs live character-skill/job-slot ESI data plus a per-character "excluded from planning" flag, and this repo's `production/esi_sync.py` deliberately never requests skill scopes at all (see that module's own SYNC.md row) since no planner existed yet to need them; `runs_ready_now`/`job_runs` is still a real, useful readiness signal without the slot split on top of it, and adding skill-scope ESI sync plus a character-management concept just to feed a slot-recommendation number is real new feature scope, not this port. The invention-needs list inside `plan_production` is ported too, now that `storage.available_blueprint_copies`/`get_blueprint_time` (both ported from the parent's real `storage.py`, verbatim including the confirmed real bug fixes each one's own docstring documents - runs summed, not copies counted; the ME0 base time) exist: every configured Tech II/III stock target gets an `InventionNeedRow`, reusing `_tech_ii_mods`' own already-chosen `InventionResult` rather than re-resolving it. `market_status`/`stock_value` are ported as genuinely simplified reads, not full three-way parity: `market_status` is a *cheap* target-vs-current-stock read with no live pricing/BOM traversal at all (matching the parent's own "a separate, simpler read" framing, just without a separate market-listed-volume column, since this repo's stock target has no market-target column distinct from the target itself); `stock_value` is ported closer to verbatim (it already needed pricing) and now also includes the manual-stock term. Added here to support all of this: `production/models.py`'s `InventionNeedRow`, `AssetPlanJob`, `MarketStatusRow` (dataclasses). CLI: `plan-asset-optimized`, `market-status`, `stock-value`, plus `plan-production`'s own printed output now includes the invention list, via `production/actions.py`'s `do_plan_asset_optimized`/`do_market_status`/`do_stock_value`. Tests: `tests/test_production_planner.py` (manual stock netting into `_current_stock`, `market_status`/`stock_value` reads, `plan_asset_optimized`'s ready-now/blocked split against a scarce shared component, its stock-on-hand-excludes-incoming-jobs behavior, and its margin gate), `tests/test_production_invention_needs.py` (the invention list's owned-BPC netting, stockpile_pct, full-coverage-is-empty case, and sort order), and `tests/test_storage.py` (manual_stock/`available_blueprint_copies`/`has_bpo_at_location`/`get_blueprint_time` round-trips, including the runs-not-copy-count fix). `discover_ship_margins`/`_scan_ship_margins` (the Margin page's ungated ship browser) are ported too, near-verbatim - every published Ship-category item with a real recipe (excluding the Special Edition Ships market-group subtree via `_descendant_market_group_ids`, ported verbatim), with home/Jita price, build cost and both margins, no `min_margin`/`min_daily_profit` gate and no history/movement lookup at all (a pure information browser, unlike `discover_build_candidates`). Not cached, same "no concurrent-tenant cache to protect" reasoning `discover_build_candidates` already uses here. Added here to support it: `production/models.py`'s `ShipMarginRow`. CLI: `discover-ship-margins`, via `do_get_ship_margins`. Tests: `tests/test_production_ship_margins.py` (not gated by margin/profit, a no-quote ship still appears with `None` prices rather than being excluded, the Special Edition exclusion, the non-ship-category exclusion, and the margin/build-cost numbers themselves). Finally, the logistics/distribution helpers were investigated individually rather than ported as one block, per the parent's real per-category-structure-assignment dependency (`storage.load_category_locations`/`ProductionConfig.distribution_source_location_id`, a whole Logistik-tab multi-structure-assignment concept this repo's config deliberately never grew - see the `config.py` row's own "still deferred" list): **`logistics_status`/`distribution_recommendations` are judged genuinely out of scope, not deferred** - both are fundamentally about routing material between *several* physical structures a solo player wouldn't realistically operate the way this repo's whole config model (one `home_location_id`, a flat two-way component/manufacturing system split - see the `config.py` row) already assumes a single build location; porting them in a "reduced single-structure form" would collapse to nothing a single-structure operator doesn't already see elsewhere (there's no "wrong structure" problem to solve when there's only one). **`invention_logistics`/`t1_bpc_invention_needs` are ported, in reduced single-location form** - unlike the Logistik tab's per-category assignments, the parent's own `invention_location_id` was already just one field ("where do you invent"), the same single-location shape `home_location_id` already has here, so `ProductionConfig.invention_location_id` (new) is a legitimate single-field port rather than a contrived multi-structure UI concept; the parent's `pull_from_location_id`/`pull_from_available` hint fields (real Logistik-tab, multi-location concepts) are dropped from the ported `LogisticsRow` accordingly. `_direct_material_mult` has no ported caller here (it only exists to feed `logistics_status`) and is left out with it. Added here to support the invention-logistics port: `production/models.py`'s `LogisticsRow`/`T1BpcInventionNeedRow`, `ProductionConfig.invention_location_id` (with a `_FIELD_RANGES` entry in the shared `config.py`, same `(1, None)` shape `home_location_id` already has there). CLI: `invention-logistics`, `t1-bpc-invention-needs`, via `do_invention_logistics`/`do_t1_bpc_invention_needs` (both re-run `plan_production` for its `invention_list`, since no "last computed plan" table exists here to read back instead). Tests: `tests/test_production_invention_logistics.py` (datacore/decryptor/BPC-run demand accumulation, the T1-blueprint-vs-Tech-III-relic availability-source split via `available_blueprint_copies` vs. `esi_stock_at_location`, zero-recommended-runs exclusion, and `t1_bpc_invention_needs`' BPO-presence check including the relic-never-has-a-BPO case). **Amended 2026-09-04, reversing this row's own earlier "out of scope, not deferred" call**: `logistics_status`/`distribution_recommendations` (GitHub issue #4) are ported now, near-verbatim, plus their shared `_direct_material_mult`/`_category_material_demand` helpers (previously left out with `logistics_status` itself). The earlier reasoning ("a solo player wouldn't realistically operate several physical structures") didn't survive contact with this repo's own real multi-character/multi-structure usage - a single eve-trader-local tenant can and does run several producer characters across several structures, the same real-world shape the parent's multi-tenant model was built to serve, so the parent's full per-category-structure-assignment concept applies here after all. Ported faithfully: `logistics_status`'s "warehouse first, then surplus" pull-from hint (informational, independent per row - GitHub issue #4's original ask) and `distribution_recommendations`' commitment-tracking `surplus_remaining` ledger (the parent's own confirmed real bug otherwise recommends double-booking the same surplus units to two short categories - both phases, warehouse-first then surplus, are covered by explicit regression tests here too, `tests/test_production_logistics.py`). Added here to support it: `storage.job_category_locations`/`category_location_options` tables with `upsert_category_location`/`load_category_locations`/`delete_category_location`/`add_category_location_option`/`load_category_location_options`/`delete_category_location_option` (all six ported near-verbatim from the parent's own `storage.py`, minus `tenant_id`/RLS - the parent's `ON CONFLICT(tenant_id, category)`/`ON CONFLICT(tenant_id, category, location_id)` become plain `ON CONFLICT(category)`/`ON CONFLICT(category, location_id)` here, matching every other ported table's tenant-column removal), `ProductionConfig.distribution_source_location_id` (falls back to `home_location_id`, same "the home market acts as the central warehouse" default the parent documents, with a `_FIELD_RANGES` entry `(1, None)` in the shared `config.py`, same shape `home_location_id`/`invention_location_id` already have there), and `production/models.py`'s `DistributionRow` plus `LogisticsRow`'s restored `category`/`pull_from_location_id`/`pull_from_available` fields (this repo's previous reduced single-location `LogisticsRow` - `type_id`/`type_name`/`location_id`/`needed`/`available`/`missing` only - is now the parent's own full shape; `invention_logistics`'s existing call site was updated to pass `category="Invention"` rather than break). **Not ported: `load_category_system_ids`/the parent's own per-job-category cost-index refinement it feeds (`_job_cost_rate`, GitHub issue #12)** - that's a separate, still-deferred divergence from this row's own `_job_cost_rate` port above (`structure_names`/system-id resolution has no analogue here at all), unaffected by this amendment; `CostIndices` here still carries only the flat component/manufacturing split. CLI: `logistics-status`, `distribution-recommendations`, `set-category-location <category> <location_id>` / `clear-category-location <category>`, `add-category-location-option <category> <location_id>` / `remove-category-location-option <category> <location_id>`, `list-category-locations` (this repo's own combined assigned+options read - no direct parent CLI equivalent, since the parent's frontend reads both tables via two separate endpoints), via `production/actions.py`'s `do_get_logistics_status`/`do_get_distribution_recommendations`/`do_set_category_location`/`do_clear_category_location`/`do_add_category_location_option`/`do_remove_category_location_option`/`do_list_category_locations` (the first two re-run `plan_production` for its `build_list`, same "no last-computed-plan table exists here" shape `do_invention_logistics` already uses; the parent's own no-op `invalidate_production_locations_cache` has no call here at all, since `_current_stock` here was never location-curated in the first place - see that function's own docstring in the parent). Tests: `tests/test_production_logistics.py` (the parent's own `logistics_status`/`distribution_recommendations` engine-level suite, ported near-verbatim against this repo's own `BuildJobEntry` field shape), `tests/test_storage.py` (category-location/option CRUD round-trips), and `tests/test_production_actions_stock.py` (the `do_*` wrappers, including the unknown-category guard and  **Amended 2026-09-03: closed the 12 remaining backend/CLI gaps a two-agent audit found undocumented against the parent (a matching GUI is a separate follow-up task, not this one).** `item_margin_detail` (new - the parent's Margin-page search for one arbitrary item, sharing `_scan_ship_margins`' home/Jita/cost-index setup but with no cache, same "cheap per-call cost profile as `build_material_tree`" reasoning) is ported, exposed via `do_get_item_margin`/CLI `item-margin`. `build_material_tree` (already ported, but never wired to anything outside its own recursive self-call) now has a real caller: `do_build_material_tree`/CLI `material-tree`. `list_owned_blueprints` (new - the parent's `do_list_owned_blueprints` grouping logic, moved here rather than kept in `actions.py`, matching this repo's own "no real logic in `actions.py`" rule) aggregates `storage.load_owned_blueprints` (new - ported verbatim) by `(type_id, is_original, ME, TE, runs)`, exposed via `do_list_owned_blueprints`/CLI `list-owned-blueprints`. A genuinely new `production/jobs.py` module (the parent has one too) adds `list_current_jobs` (ported near-verbatim, reading the new `storage.list_industry_jobs`/`get_product_quantity`) and a **deliberately reduced** `character_slot_overview`: the parent's version needs a live ESI character-skills pull (`esi-skills.read_skills.v1`) to derive each character's real total/free job-slot count and an `excluded_from_planning` toggle that only ever fed its own not-ported `plan_asset_optimized` slot-splitting - neither exists here (see `esi_sync.py`'s own SYNC.md row for why skill scopes were never requested), so this repo's `CharacterSlotRow` carries only `used_slots` (currently-running job count) per character/category, derived entirely from already-synced industry-job data - still genuinely useful ("how many jobs is each character running right now") without a total to compare against. Exposed via `do_list_current_jobs`/`do_character_slot_overview`, CLI `list-current-jobs`/`character-slots`. The manual Build/Buy override table is ported too (`storage.manual_build_buy`, distinct from the still-not-ported manual-decryptor-override table - this one skips the buy-vs-build cost comparison entirely for a `type_id`, that one only picks which decryptor a Tech II/III item uses): `upsert_manual_build_buy`/`load_manual_build_buy`/`delete_manual_build_buy`, and - unlike a bare CRUD table nobody reads - it's actually wired into `_buy_or_build_decision`/`_base_runs`/`_expand_all` (a new optional `manual_overrides` parameter on each, default `None` so every existing caller keeps its old all-cost-based behavior) and consulted by `plan_production`/`plan_asset_optimized`/`plan_special_order` (a forced override also skips both the `min_margin` gate and the overbuild buffer for that `type_id`, matching the parent's own reasoning - a manual decision already says exactly how much to source, so re-applying either would second-guess a choice made on purpose). Exposed via `do_set_manual_build_buy`/`do_clear_manual_build_buy`/`do_list_manual_build_buy`, CLI `set-manual-build-buy`/`clear-manual-build-buy`/`list-manual-build-buy`. `do_update_stock_target` (new - GitHub issue #16 parity, "edit like the Doctrine table") edits an existing stock target's `quantity`/`jita_target` in place; unlike the parent's COALESCE-based partial update (this repo's simpler two-column `stock_targets` table has no such per-column upsert), a field left unset is filled in from the row's current value in Python before calling the existing `storage.upsert_stock_target`, and it raises `ActionError` if the row doesn't already exist (that's what `do_add_stock_target` is for). CLI: `update-stock-target <item> [--quantity] [--jita|--home]`. Finally, `do_get_system_cost_indices` (new thin wrapper exposing the already-ported `pricing.system_cost_indices_for` standalone, a Settings sanity-check display) and `do_estimate_invention` (new - resolves a bare product name via the new `storage.find_invention_recipe_by_product_name` to the *product* half of the recipe only, never the arbitrary non-deterministic blueprint-grade half, before calling the already-ported `invention.compare_recipes_and_decryptors`/`best_recipe_for_decryptor` - matching the parent's own confirmed fix for a real bug where taking the wrong half could silently compare a different Tech III relic grade on every call) are both exposed via CLI (`system-cost-indices`, `estimate-invention <product> [--decryptor]`). `storage.search_item_stock_locations` (new - ported verbatim: groups already-synced character/corp assets for one `type_id` by `(resolved_location_id, owner)`) is exposed via `do_search_item_locations`/CLI `item-locations <item>`, resolving the item the same name-or-type_id way `do_add_stock_target` does. It reads location names from a new, genuinely shared `structure_names` cache table (`location_id -> name`, `None` meaning "resolution attempted and failed" vs. no row at all meaning "never attempted" - same `get_cached_structure_name`/`set_cached_structure_name` shape as the parent) resolved via `do_resolve_structure_name` (new - tries each registered producer character's corp structure list first via `ESIClient.corporation_structures`, falling back to per-character docking history via `get_structure_name`; both already existed in `esi_client.py`, only nothing here called them yet), CLI `resolve-structure-name <location_id> [--force]`. One shared table, not two, matching the parent's own single `structure_names` table used by both this and Trading's wallet-transactions location lookups (this repo's own `do_wallet_transactions` doesn't resolve location names yet, so only Production's use is wired up for now, but the table itself is the same shared shape so Trading can adopt it later without a second table). Tests: `tests/test_production_gaps.py` covers all of the above. |
| `eve_trader_local/production/config.py` | `eve_trader/production/config.py` | The `ProductionConfig` **field definitions** that are ported (defaults included), and the `validate_production_overrides` enum layer: the structure/rig fields are checked against `production/constants.py`'s `STRUCTURE_TYPES`/`RIG_TIERS`, on top of the generic type/range checks, because a hand-edited config.yaml bypasses any dropdown and a typo'd structure name would otherwise pass the plain string check and blow up later as a bare `KeyError` inside the build-cost math. | Ported 2026-09-02. **Deliberately a subset.** Ported: `jita_buy_broker_fee`, `haul_cost_per_m3`, `home_market`, `home_location_id` (all four read by `pricing.py`), the three invention skill levels (`encryption_skill_level`/`datacore_skill_1_level`/`datacore_skill_2_level`, read by `invention.skill_multiplier`, each with a `_FIELD_RANGES` entry bounded 0-5 — EVE's own hard skill-level ceiling, not an app choice), plus the six `*_structure_type`/`*_rig_tier` fields — those have no `pricing.py` consumer yet, but `constants.structure_rig_multiplier` (already ported) is the function they exist to feed, and they are the entire reason the enum-validation layer exists, so porting the layer without its fields would have been half a port. **Amended 2026-09-02** with the fields `engine.py`'s cost/margin port reads: `market_fees` (margins), `facility_tax_rate` and the three `*_cost_index_override`s (`_job_cost_rate`, all four range-checked in the shared `config.py` the same way), and `component_system_id`/`manufacturing_system_id` (the 2-way system split `_job_cost_rate`/`_security_multiplier_for` resolve their cost index and rig-security multiplier from). **Amended 2026-09-02** with `min_margin` (0.15 default, matching the parent's own stock-target-planner default) and `min_daily_profit` (0.0 default — no floor), both range-checked `(0, None)` in the shared `config.py` for the same "a value above 100%/any absolute figure is a legitimate setting, never a typo" reasoning `min_margin_threshold`/`min_profit_threshold` already use there — added for `engine.discover_build_candidates`, not the stock-aware planner the parent originally scoped `min_margin` to: discovery needs the exact same "is this worth building at all" gate a configured stock target would need, and this repo has no stock-target table yet for the planner's own copy of that gate to attach to, so there is only one `min_margin` field here doing both jobs until the planner exists. **Amended 2026-09-03** with `component_overbuild` (0.7 default) for the now-ported stock-aware planner's overbuild-buffer math (`engine._expand_all`), range-checked `(0, None)` in the shared `config.py`. **Amended 2026-09-03** with `invention_location_id` (new field, not in the parent's own "logistics tabs, web-UI-shaped" deferral list any more - unlike `distribution_source_location_id`, it's a single "where do you invent" location, the same shape `home_location_id` already has here, not a multi-category Logistik assignment; see the `engine.py` row's own reasoning), with a `_FIELD_RANGES` entry `(1, None)` in the shared `config.py`, same shape as `home_location_id`. **Amended 2026-09-04** with `distribution_source_location_id` (falls back to `home_location_id`), range-checked `(1, None)` in the shared `config.py`, same shape as `home_location_id`/`invention_location_id` - see the `engine.py` row's own reasoning for why `logistics_status`/`distribution_recommendations` are ported now rather than left out of scope. Still deferred, each with the module that will need it: `bpc_inventory` (the parent's own not-yet-ported BPC-inventory-target field), `component_system_name`/`manufacturing_system_name` (the parent resolves those to the `_id` fields in a web-only Settings action; a local install sets the id directly), and `fuzzwork_csv_base` (already a module constant in this repo's `sde.py`). **Layering, matching the parent's:** the shared `config.py` must never import this module — `validate_config_overrides`/`apply_config_overrides` are reused from it, and the Production-only enum checks are layered on top here, since they need `production/constants.py`. The one exception is the `_FIELD_RANGES` entries for `haul_cost_per_m3`/`home_location_id`, which live in the shared `config.py` exactly as they do in the parent's: `_check_range` is keyed by field *name* and needs no Production import to do it. Storage scope key is `"production"` (the parent's own), so Trading and Production overrides can never collide. No `ConfigProxy`/contextvars: single user, one config — see this repo's `config.py` docstring. |
| `eve_trader_local/production/pricing.py` | `eve_trader/production/pricing.py` | The material-acquisition half of buy-vs-build: `home_prices`/`jita_prices` (ESI order-book first, Goonmetrics current-price snapshot only as a lazy fallback), `_candidate_prices`/`buy_source`/`buy_price` (home sell price vs. Jita sell price + haul cost, both inflated by the buy-side broker fee, only the Jita side hauled) and `system_cost_indices_for`. | Ported 2026-09-02. The two decisions worth keeping in sync are both in the code: **the Goonmetrics fallback is only ever fetched on an actual ESI failure**, never eagerly alongside the ESI attempt (the success path would otherwise pay for a multi-megabyte download it throws away, and an unset `home_location_id` would still hit the network), and **`buy_source` and `buy_price` share one `_candidate_prices` helper** so the reported source can never disagree with the reported price. A zero sell price means "nothing listed", never "free" — covered by its own test, since `region_order_stats_bulk` reports an unlisted type as an empty `OrderStats`. Adapted, not ported: the parent resolves producer characters via `production/esi_sync.list_producer_characters()`; here it is `TokenManager.list_records("producer")`, this repo's own role-prefix convention (a `producer` role, deliberately not Trading's `seller` — the character who can dock at the industry structure isn't necessarily the one running sell orders). Left out: the parent's `production/jita_price_cache` hourly snapshot (a pure perf layer for its web `plan_production` path; nothing here calls this in a loop yet — port it with the planner if the same cost shows up). Jita's region id still comes from `TradingConfig`, not `ProductionConfig`, matching the parent. Source labels are `"home"`/`"jita"` where the parent returns `"C-J"`/`"Jita"` — the parent's are its own structure's nickname baked into a return value; anything that switches on them must be ported with that in mind. Broad `except Exception` narrowed to `(ActionError, requests.RequestException)` — `ESIError` subclasses `ActionError` here, and a token that can no longer be refreshed raises `ActionError` too. |
| `eve_trader_local/production/invention.py`, `eve_trader_local/production/models.py` | `eve_trader/production/invention.py`, `eve_trader/production/models.py` | The whole invention calculation: `skill_multiplier` (EVE's real formula — each of the two datacore/science skills is worth 1/30 per level and the encryption skill 1/40, all three *added*, never multiplied together), `estimate`'s per-attempt datacore/decryptor/relic pricing with the 100%-probability cap and the `max(0, base_runs + run_bonus)` output-run count, `reducible_material_cost`'s qty>1 rule, and the four selection helpers (`compare_decryptors`/`best_decryptor_for_item` for one fixed invention source, `compare_recipes_and_decryptors`/`best_recipe_and_decryptor` across every source, and `best_recipe_for_decryptor` for a hand-picked decryptor with an auto-optimised source). Plus the `InventionResult` dataclass. | Ported 2026-09-02, near-verbatim. Three real decisions came over in the code, not just in this table: **the ranking figure is net cost per BPC run**, i.e. `(attempt cost / probability) / output_runs - (ME/100) * reducible_material_cost_per_run` — a decryptor's ME is worth real ISK on every subsequent build, so ranking on raw invention cost alone picks the wrong decryptor; **an input with no sell order anywhere is "unknown", never 0 ISK** (`all_prices_known` gates `expected_cost_per_success`/`_per_run`/`net_cost_per_run` to `None`, so a missing price can't silently make an expensive decryptor look cheap — a confirmed parent bug, 2026-08-29); and **a Tech III Sleeper relic IS priced into the attempt cost while a T1 blueprint deliberately is not** (a relic must be bought/looted fresh per attempt, a T1 BPC is a near-free reprint of an owned BPO — omitting the relic overstated Tech III profitability, reported by a user 2026-08-30). Tech III's up-to-three relic grades (Intact/Malfunctioning/Wrecked, materially different odds *and* output runs *and* price) are compared side by side rather than always assuming the best-odds grade. Adapted: the parent's bare `ValueError` for "no recipe/no probability row" is a named `NoInventionRecipe(ActionError)` here — the multi-candidate helpers catch it to skip an unusable candidate, so it has to be narrower than a bare exception, and it still prints as a plain message if it reaches the user; an unknown decryptor name raises `ActionError` instead of a bare `KeyError`. Added here to support it: `storage.get_invention_recipe` and `storage.get_blueprint_materials` (both read tables `sde.py` already populates; the parent's `lru_cache` dropped, same call this repo made for every other SDE read — the parent added it there purely for its own planner's ~12-queries-per-item hot loop, which doesn't exist here yet), a new `production/models.py` (mirroring the parent's own Trading/Production dataclass split), and `ProductionConfig`'s three invention skill fields. The parent's job-installation-fee and T1-BPC-copy-cost omissions are kept as-is and documented in the module docstring — they're consistent across every decryptor for the same item, so they don't change *which* one is recommended except in genuinely marginal calls. |
| `eve_trader_local/production/esi_sync.py`, `eve_trader_local/production/actions.py` | `eve_trader/production/esi_sync.py`, `eve_trader/production/actions.py` (`do_sync_esi`) | The producer-character ESI sync: `PRODUCTION_SCOPES`, `list_producer_characters`, the three row builders, `_normalize_activity_id`, `_fetch_character_data` and `sync_esi`'s two-phase shape — a parallel per-character Phase A (independent, latency-bound) and a deliberately *sequential* Phase B for corp-level data, which is stateful across characters (each corp fetched once, retried with the next character sharing it when an earlier one lacks Director). Plus its failure contract: every ESI call is individually isolated so one missing scope or role reports as a skipped character/corp instead of aborting the sync. | Ported 2026-09-02. Two real EVE/ESI quirks came over verbatim because nothing else in either repo encodes them: **live industry jobs report Reactions as activity_id 9 while the SDE files every reaction recipe under 11** (normalized once at ingestion, so every downstream SDE lookup keeps working — has its own test), and **ESI's blueprint sentinels**, `runs = -1` for an Original and `quantity = -2` for a copy, which is what makes `get_owned_bpo_best_me_te`'s BPO-only filter correct rather than arbitrary. **Adapted, not ported: the parent's `storage.with_current_tenant` wrapping of the thread pool has no analogue here** (no tenancy, and `storage.connect()` already opens a fresh connection per call, so nothing is shared across threads) — but the parent's *reason* for it, a worker thread refreshing an expired token, still bites: this repo's `TokenManager` deliberately has no refresh lock. So `_usable_characters` refreshes every token up front on the calling thread and drops the ones that fail, which both keeps refresh off the pool and turns a dead refresh token into a reported "skipped" character. **Deliberately narrower than the parent's `sync_esi`:** the sell-order, character-skill/job-slot and structure-name-discovery phases are left out with the features that consume them (Trading's `own_orders.py` already covers live sell orders; job slots only feed the not-yet-ported planner; structure-name discovery feeds the parent's web-only Logistik per-category structure assignments) — so `PRODUCTION_SCOPES` asks only for what is actually called, plus `esi-markets.structure_markets.v1` for `pricing.py`'s home order book. Added here to support it: six snapshot tables (`character_`/`corp_` × `assets`/`blueprints`/`industry_jobs`), `storage.replace_assets`/`replace_blueprints`/`replace_industry_jobs` with the parent's `resolved_location_id` container-chain walk (`_resolve_locations`; resolving only one level was a confirmed parent bug that made stock inside a container inside a corp hangar read as absent), and their three readers `esi_stock_at_location`, `esi_incoming_industry_qty` and `get_owned_bpo_best_me_te`. `PRODUCER_ROLE_PREFIX` moved here from `pricing.py`, matching the parent's direction (`pricing.py` reaches into `esi_sync` for it). `production/actions.py` is new (mirroring the parent's own Trading/Production actions split) and holds `do_sync_esi`/`do_get_esi_sync_time`/`do_list_producer_characters`/`do_remove_producer_character`; the parent's two cache invalidations in `do_sync_esi` have no local counterpart yet (neither cache exists here). CLI: `sync-esi`, and `auth --role producer` picks `PRODUCTION_SCOPES`. |
| `eve_trader_local/production/engine.py`'s `plan_special_order`, `production/actions.py`'s `do_*_special_order` family, `production/models.py`'s `SpecialOrder`/`SpecialOrderLineItem`/`StockOverlapWarningRow`, `storage.py`'s `special_orders`/`special_order_items` tables | A newer feature (added in eve-trader after this repo's planner port, 2026-09-03) for pricing an ad-hoc, manually-entered list of items (not tied to a standing stock target). `plan_special_order` was factored out of `plan_production` specifically so both share the same BOM/pricing core, `net_against_stock` toggling whether it treats the order as "from scratch" or nets against currently-synced assets/incoming jobs. | Ported 2026-09-03. Reuses `plan_production`'s own already-ported internals directly (`_base_runs`/`_expand_all`/`_current_stock`/`structural_material_closure`) rather than duplicating them - the only change needed in shared code was a new `ignore_current_stock` parameter on `_expand_all` (the "from scratch" mode nets nothing at all, matching the parent's own `_expand_all` shape) and factoring `plan_production`'s inline buy_list/build_list construction out into two small helpers (`_build_buy_list`/`_build_build_list`) so both planners build the exact same row shape from the same `buy_totals`/`build_runs`/`gross_demand` inputs without a second copy of that loop - the same reasoning the parent's own `_build_buy_list`/`_build_build_list` split documents. Two deliberate differences from `plan_production`, matching the parent's confirmed behavior: no margin gate (an order must be fulfilled regardless of `cfg.min_margin`) and `net_against_stock`'s `stock_overlap_warning` (every item reachable from both this order's tree and the configured stock targets' tree that currently has stock on hand - a heads-up, not a precise "did the regular plan actually consume it" check). **Real, deliberate simplification from the parent, matching `plan_production`'s own already-documented gaps, not a new divergence**: no invention-needs list (needs `storage.available_blueprint_copies`/`get_blueprint_time`, neither ported here) and no manual-stock override table (ESI sync is the only stock source, via `_current_stock`) - so `plan_special_order`'s result dict has no `invention_list` key at all, unlike the parent's. Storage: `special_orders`/`special_order_items` ported from the parent's `docs/special_orders_schema.sql` minus `tenant_id`/RLS, same "plain TEXT UUID generated in Python" convention the Doctrine tables already use, and the same natural `(order_id, type_id)` key on `special_order_items` the parent's own schema comment gives (a type_id appears at most once per order). CLI: `create-special-order <item:qty> [<item:qty> ...] [--note] [--net-against-stock]` (accepts either a name or type_id per item, resolved the same way `set-stock-target` does - the parent's own `do_create_special_order` takes pre-resolved type_ids only, since it's fed by a frontend item picker with no CLI equivalent here) / `list-special-orders` / `update-special-order <order_id> [--status] [--note]` / `remove-special-order <order_id>` / `compute-special-order <order_id>`, via `production/actions.py`'s `do_create_special_order`/`do_list_special_orders`/`do_get_special_order`/`do_update_special_order`/`do_remove_special_order`/`do_compute_special_order` (same set as the parent's). Tests: `tests/test_production_special_orders.py` (CRUD round-trip, creating an order, computing it with `net_against_stock=True` vs `False` and confirming the netting actually changes the result against synced stock, the stock-overlap warning firing against a configured stock target, and no-margin-gate behavior). |

### Doctrine

A separate tool from Trading/Production in the parent repo too - the three
share only the SDE/ESI/config foundation. Fully ported now: the EFT-fitting
parser, its supporting constants/dataclasses, the pure contract-matching/
stockpile-deviation scoring logic (`validation.py`), ESI contract + asset
sync, and the orchestration layer (`engine.py`/`actions.py`) that wires
everything together against real synced data, including the Shopping
List's Build-vs-Buy-C-J-vs-Buy-Jita comparison (ported 2026-09-03, see the
`engine.py`/`actions.py` row below).

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/doctrine/constants.py` | `eve_trader/doctrine/constants.py` | The whole file: the nine EFT slot sections plus the local `fuelbay`/`shipmaintenancebay` addition (GitHub issue #18), `EXACT_SECTIONS`/`CONSUME_SECTIONS`, the SDE category IDs (`SHIP_CATEGORY_ID`/`CHARGE_CATEGORY_ID`/`DRONE_CATEGORY_ID`/`STRUCTURE_CATEGORY_ID`/`FIGHTER_CATEGORY_ID`/`VALID_HULL_CATEGORY_IDS`), the parse-issue/deviation/severity/validation-status/ampel vocabularies, the ESI contract-sync status sets, and the B.5 matching weights/threshold. All of it is fixed, curated business data with no config or storage dependency, so it's pure sync material. | Ported 2026-09-03, verbatim (in full, same "one flat table, don't split it" reasoning `production/constants.py`'s own row uses). Deliberately duplicates `SHIP_CATEGORY_ID` rather than importing it from `production/constants.py` - matching the parent's own documented choice (see this file's module docstring) to avoid a doctrine<->production dependency edge that doesn't otherwise need to exist. |
| `eve_trader_local/doctrine/models.py` | `eve_trader/doctrine/models.py` | The whole file, ported in full rather than split - same reasoning as `constants.py`'s row above. Only `ParsedItem`/`ParsedIssue`/`ParsedFitting` (parser.py's actual return shape) and the master-data shapes (`Doctrine`, `Fitting`, `FittingItem`, `ParseIssue`) have a consumer in this port; the sync/results and status/ampel dataclasses (`ContractRow`, `StockpileRow`, `FittingStatus`, ...) come along for the same "don't re-derive the split later" reason and wait for the module that will actually build them. | Ported 2026-09-03, verbatim apart from the module docstring, which no longer claims a `api/schemas.py` Pydantic mirror (this repo has no web API layer at all - see `ROADMAP.md`). |
| `eve_trader_local/doctrine/parser.py` | `eve_trader/doctrine/parser.py` | The whole EFT fitting-format parser: `parse_fitting`'s full grammar (header/body/blocks, `_EMPTY_MARKER_RE`/`_OFFLINE_SUFFIX_RE`/`_QTY_SUFFIX_RE`, the position-vs-SDE "gegenprobe" gated on `has_markers`), `_resolve_names`' right-to-left comma-split disambiguation, `_classify_section`'s qty-suffix-overrides-SDE-slot rule, `_levenshtein`/`_suggest`'s "did you mean" hull/item-name suggestion (real fuzzy matching is never used for actual resolution, only for a failed-lookup hint), and `parse_bay_items` (GitHub issue #18's Fuel Bay/Ship Maintenance Bay plain-text lists, entered outside the main EFT grammar since CCP's own Fitting Formats spec has no syntax for either). Plus the `ResolvedType` dataclass and `FittingParseError`. | Ported 2026-09-03, verbatim - genuinely pure, storage-free text parsing (confirmed by reading it, not assumed): every SDE lookup arrives as an injected `resolve_name`/`resolve_slot` callable, so the module itself imports nothing beyond `.constants`/`.models`. **`validation.py` coupling, checked and confirmed clean**: `doctrine/validation.py` (not ported) consumes `FittingItem`/`ContractItemRow`/`DeviationRow` - parser.py's *output* type, one step downstream - and neither module imports the other; `parser.py` produces structured data, `validation.py` is a wholly separate later pass over already-parsed/already-synced rows. Porting the parser alone leaves nothing half-working. Added here to support real (non-test-fake) resolver wiring: `storage.get_type_slot`, `storage.resolve_sde_type_by_name` and `storage.list_hull_type_names`, ported from the parent's `storage.py` (all three are pure SDE reads over tables `sde.py` already populates here - `sde_type_slots`/`sde_types`/`sde_groups` - with no tenant/config dependency; the parent's `lru_cache` on `get_type_slot` dropped, same call this repo made for every other SDE read). The real wiring these resolvers exist for (`doctrine/engine.py`'s `_resolve_name`/`_resolve_slot`/`parse_fitting_text`) is itself still a "Candidate" below - CLI: `parse-fitting <path>`, a standalone smoke-test command (no `engine.py`/`actions.py` exists yet to own the wiring, so it's inlined in `cli.py`) proving the ported parser resolves real SDE data correctly, following the same "prove the layer works" precedent as `whoami`/`config`. Tests: `tests/test_doctrine_parser.py` (the parent's own fake-resolver unit tests carried over near-verbatim, plus new coverage for quantity/drone/cargo classification, malformed input, CRLF normalization, the position-vs-SDE gegenprobe, `parse_bay_items`, and one integration pass against a real `storage.replace_sde_data`-seeded cache) and `tests/test_doctrine_models.py` (dataclass defaults/mutable-default-list safety for the parser-output shapes). |
| `eve_trader_local/doctrine/validation.py` | `eve_trader/doctrine/validation.py` | The whole file, verbatim: B.2/C.1 Soll construction (`build_contract_soll`'s exact/consume split with the charge-floor-of-1 merge, `build_stockpile_soll`'s hull-included multiset times `total_sets` including issue #36's additive contract-shortfall demand), B.2/B.3 the hull-gate/Ist multiset (`hull_gate_satisfied` deliberately not requiring `is_singleton` - a real fitted ship's contract-items response isn't a reliable packaged-vs-assembled signal there, unlike the asset endpoints; `build_contract_ist` summing separately-stacked damaged charges, since ESI's contract-items schema has no damage field to filter on at all), B.4 `compute_deviations`'s full missing/short/extra table plus is_included=False "requested consideration" extras, B.5 `match_score`'s weighted multiset-overlap and `clears_match_threshold`, B.6 `pair_wrong_variants`' same-group/same-slot greedy re-labeling, B.7 `contract_status`, C.2/C.3 `allocate_stockpile`'s greedy priority allocation and `stockpile_deviation`, and B.8's `contract_ampel`/`stockpile_ampel`/`worst_ampel`/`worst_severity` aggregation. | Ported 2026-09-03, verbatim apart from the module docstring (no `engine.py` exists here yet to be its caller - noted as currently caller-less beyond its own test suite, confirmed cleanly separable from `parser.py` by reading the real code, same finding the parser row above already recorded). No new SDE reads or `DoctrineConfig` fields needed - every input (`FittingItem`/`ContractItemRow`/`DeviationRow`, plus plain scalars like `cargo_tolerance_pct`) is already-loaded data the caller supplies; `pair_wrong_variants` takes `group_id_of`/`slot_of` as injected callables, same "caller does the real lookup" shape as `parser.py`'s own resolvers. Storage-free by construction, matching the parent's own design constraint (module docstring), not a repo-specific choice - nothing to decide here. Tests: `tests/test_doctrine_validation.py`, the parent's own suite carried over verbatim apart from the import path (45 cases: exact/consume Soll construction including the charge-merge and issue #36 additive-shortfall cases, hull-gate/Ist, all of B.4's deviation branches including strict-extras and not-included "requested consideration", B.6 wrong-variant pairing across matching and non-matching groups, B.7 status, B.5 match scoring, C.2/C.3 stockpile allocation/deviation, and every B.8 ampel/severity aggregation branch). |
| `eve_trader_local/doctrine/esi_sync.py`, `eve_trader_local/doctrine/engine.py` | `eve_trader/doctrine/esi_sync.py`, `eve_trader/doctrine/engine.py` | The real-SDE resolver wiring (`_resolve_name`/`_resolve_slot`/`parse_fitting_text`/`parse_bay_items_text`), Fitting/Doctrine row<->dataclass mapping, `load_match_candidates`/`match_and_validate_contract`'s per-contract matching (with the `NO_HULL_MATCH` sentinel never persisted), `stockpile_rows_for_doctrine`/`aggregate_stockpile_rows`' Soll/Ist/allocation orchestration, and `fitting_status`/`doctrine_status`'s ampel rollups - plus the ESI contract-sync flow itself: `_passes_prefilter`/`_issued_by_own_identity`'s pre-items-fetch filtering, the "carry forward an unchanged contract's already-fetched items" optimization, and the asset sync (Stockpile's Ist side). | Ported 2026-09-03. **Deliberate structural simplification, confirmed by reading the parent's actual role split rather than assumed**: the parent keeps contract-reading characters (`DOCTRINE_ROLE_PREFIX`/`DOCTRINE_SCOPES`) and asset-scanning characters (`DOCTRINE_ASSET_ROLE_PREFIX`/`DOCTRINE_ASSET_SCOPES`) as two entirely separate character-auth groups, reasoned there as letting a multi-tenant Doctrine-only tenant use Stockpile without ever touching Production, and as not asking a contracts-only character for asset scopes it doesn't need. Neither reason survives contact with a single-user local install (there is only ever one person, and EVE SSO shows every requested scope on one consent screen regardless of how many role prefixes they're split across) - so this repo merges both into one `DOCTRINE_ROLE_PREFIX`/`DOCTRINE_SCOPES` pair and one `sync_doctrine()` entry point (`sync_contracts`/`sync_assets` still exist underneath, independently callable and independently isolated - a failure in one half doesn't discard the other's already-persisted result). `fitting_status`'s `multibuy_cost` is therefore always `None` here, never wired to a live home-price lookup. **Update 2026-09-03**: GitHub issue #19's `doctrine_contract_history` (a permanent, append-only "who bought what and when" log, independent of `doctrine_contracts`' own active/wholesale-replaced snapshot) is now ported too - `esi_sync.sync_contracts` additionally scans the same already-fetched raw contract lists for `FINISHED_CONTRACT_STATUSES` (`_passes_history_filter`), reuses whichever `fitting_id` that contract was already matched against while still outstanding (never re-matched from items, which are immutable once finished), and upserts one denormalized row (fitting_name/hull_type_id snapshotted at record time, so the row stays meaningful even after that fitting is later edited/deleted) per newly-finished contract, keyed by `contract_id` alone (single-user, no tenant-collision PK widening needed). **Update 2026-09-03**: the Shopping List's Build-vs-Buy-C-J-vs-Buy-Jita comparison (`shopping_list_rows`/`_shopping_prices`) is now ported too, reusing Production's real cost engine directly (`production/engine.py`'s `unit_cost_detail`/`_haul_volume`/`structural_material_closure`, `production/pricing.py`'s `home_prices`/`jita_prices`) so a user's owned-BPO/ME/TE/structure config gives the exact same build-cost number here as in Production itself - same reasoning as the parent's own import comment in `doctrine/engine.py`. One deliberate simplification versus the parent: no separate live-order-book presence check (the parent's GitHub issue #10 fix) - `home_prices`/`jita_prices` are themselves already ESI order-book-first here (unlike the parent's Goonmetrics-first shape those functions had when issue #10 was filed), so the bug that check exists to catch can't reach this function in the first place. `DoctrineConfig.import_cost_per_m3` is new, deliberately independent of `ProductionConfig.haul_cost_per_m3`, matching the parent's own field. Added here to support it: the `doctrines`/`doctrine_fittings`/`doctrine_fitting_items`/`doctrine_fitting_parse_issues`/`doctrine_contracts`/`doctrine_contract_items`/`doctrine_contract_deviations`/`doctrine_character_assets`/`doctrine_corp_assets` tables (`storage.py`, ids are Python `uuid.uuid4()` strings rather than Postgres's `gen_random_uuid()`) with their full CRUD, plus `storage.esi_stock_at_location`'s new `tables` parameter (defaulting to Production's own asset tables, so that port's callers are unaffected) so Doctrine's stockpile computation can point it at `storage.DOCTRINE_ASSET_TABLES` instead - Doctrine keeps its own independent synced-asset cache, exactly like the parent, so Stockpile works even if Production was never set up. `DoctrineConfig` (`doctrine/config.py`) is new too: `doctrine_structure_id`/`stockpile_location_id` (each falling back to `TradingConfig.structure_id` via `effective_structure_id`/`effective_stockpile_location_id`), `cargo_tolerance_pct`, `strict_extras` - loaded/saved exactly like `ProductionConfig` (defaults -> config.yaml -> stored overrides, scope key `"doctrine"`). CLI: `auth --role doctrine`, `create-doctrine`/`list-doctrines`, `add-fitting`/`list-fittings`, `sync-doctrine`, `validate-contracts`, `doctrine-status`/`stockpile-status`, `shopping-list`, `list-contracts`, `contract-history`. Tests: `tests/test_doctrine_engine.py` (real-SDE parser wiring, Soll construction from a stored fitting, exact-match/no-hull/missing-position matching, stockpile shortfall against synced assets, aggregation, status rollups) and `tests/test_doctrine_esi_sync.py` (structure-not-configured/no-character guards, a full parse-fitting-then-sync-a-matching-contract flow asserting the persisted snapshot, hull-absent and wrong-structure filtering, per-character scope-failure degradation, asset sync, and `sync_doctrine`'s per-half isolation). `tests/test_doctrine_contract_history.py` (issue #19, added 2026-09-03: a finished contract producing a history row, a never-matched finished contract *not* being recorded, history surviving a later sync that drops the contract from ESI's own list entirely, `upsert`'s update-in-place-on-conflict behavior, `do_contract_history`'s hull/character-name resolution and `doctrine_id` filter, and NULLS-LAST ordering). |
| `eve_trader_local/doctrine/actions.py` | `eve_trader/doctrine/actions.py` | The Doctrine-relevant `do_*` subset: `do_create_doctrine`/`do_update_doctrine`/`do_delete_doctrine`/`do_list_doctrines`, `do_parse_fitting`/`do_add_fitting`/`do_update_fitting`/`do_delete_fitting`/`do_list_fittings`/`do_get_fitting_detail` (with issue #18's `_merge_bay_items` line-numbering), `do_sync_doctrine`, `do_validate_contracts`, `do_get_doctrine_status`/`do_get_stockpile_status`/`do_get_shopping_list`/`do_list_contracts`, and `do_update_settings`. Same thin-orchestration/`ActionError`-boundary shape as `actions.py`/`production/actions.py`. | Ported 2026-09-03. Deliberately excluded, matching `engine.py`'s own deferrals: `do_sync_assets`/`do_get_asset_sync_time` as separate actions (folded into `do_sync_doctrine`), `do_contract_history` is now ported too (2026-09-03) - resolves `hull_name`/`source_character_name` at read time same as `do_list_contracts`, plus an optional `doctrine_id` filter (a history row only carries `fitting_id`, so this is a post-filter over `storage.get_fitting` rather than a query-level one). CLI: `contract-history [doctrine_id]`. `do_get_shopping_list` is now ported too (2026-09-03), thin as ever - see the `engine.py`/`esi_sync.py` row above for what backs it. `do_list_doctrine_characters`/`do_remove_doctrine_character` are kept even though there's no dedicated Doctrine CLI command for them yet (`auth --role doctrine`/`whoami` already cover login/listing) - cheap, and a future GUI's character-management view wants them directly. CLI wiring lives in `cli.py`'s `cmd_*` functions (see the row above for the full command list); `main()` was also fixed to call `production.config.reload()` on every invocation, alongside the new `doctrine.config.reload()` - a pre-existing gap (Production Settings overrides were never actually re-applied on a fresh CLI process before this) that would have silently affected Doctrine's own settings too if left alone. Tests: `tests/test_doctrine_actions.py` (duplicate/blank doctrine names, unknown-doctrine/negative-target guards, add/update/delete fitting including update's re-parse-and-revalidate-contracts behavior and delete's unmatch-don't-delete contract rule, doctrine cascade-delete, fitting detail assembly, stockpile status before any asset sync, and settings validate-before-apply) and `tests/test_doctrine_shopping_list.py` (a shortfall item priced all three ways with the cheapest correctly identified, and an unpriceable item - no build recipe, no market data on either leg - coming back with `recommended_source`/`total_cost` both `None`, never a silent 0 ISK). |

### Ore & Minerals

A separate tool from Trading/Production/Doctrine in the parent repo too
(GitHub issue #90) - the four share only the SDE/ESI/config foundation. Fully
ported now: the fixed reprocessing-yield constants, the actual ore/ice +
scrapmetal yield math, the inventory-paste parser (issue #92), both
live-pricing quote calculations (the Ore Shortlist's per-row profit math,
issue #91, and the Reprocessing-tab's sell-as-is-vs-refine recommendation,
issue #92), the candidate discovery universe, the Mineral Shopping List's LP
optimizer (issue #93), and now the orchestration layer (`actions.py`) that
wires everything together against real ESI/Goonmetrics prices and CLI
commands.

**Dependency note**: `refining/optimizer.py` is the one module in this repo
that needs anything beyond `requests`/`PyYAML` - it adds **`scipy`** (and
`numpy`, which comes with it) to `pyproject.toml`, for
`scipy.optimize.linprog`'s HiGHS solver. A deliberate, one-off exception to
this repo's dependency-light discipline (the same call the parent made for
the same module): the shopping list is a genuine linear program, and
hand-rolling a simplex implementation is notoriously easy to get subtly
wrong. Nothing else here imports scipy, and nothing else should without the
same justification.

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/refining/constants.py` | `eve_trader/refining/constants.py` | The whole file, verbatim (same "one flat table, don't split it" reasoning `production/constants.py`'s own row uses): `BASE_YIELD_POINTS`/`STRUCTURE_YIELD_MODIFIER`/`RIG_YIELD_BONUS_POINTS`/`REPROCESSING_IMPLANT_BONUS`/the three per-level skill-bonus constants (the ore/ice path), `SCRAPMETAL_BASE_YIELD`/`SCRAPMETAL_SKILL_BONUS_PER_LEVEL` (the scrapmetal path), the compression constants (`ORE_ICE_CATEGORY_ID`/`ICE_GROUP_IDS`/`COMPRESSED_VOLUME_RATIO_*`), `_rounded_security`/`security_yield_modifier`/`clamp_skill_level` and the three `*_options()` helpers. All fixed, curated real-EVE game data with no config or storage dependency, so it's pure sync material. | Ported 2026-09-03, verbatim apart from two docstring cross-references (see the reprocessing.py row below for why - this repo folds the parent's separate `refining/engine.py` into its own `reprocessing.py`, so the two "see refining/engine.py" pointers were updated to name that file instead; the formulas/values themselves are untouched). Carries forward the module docstring's own confirmed-historical-bug context intact: the ore/ice formula's structure/rig/security component was originally guessed as a back-solved additive term before the real live wiki formula was confirmed (off by ~0.1-0.2pp even at its one confirmed data point), and the commonly-quoted "2%/level" Scrapmetal Processing figure is confirmed wrong against the real 55% skill-maxed ceiling (1%/level is what reconciles). |
| `eve_trader_local/refining/models.py` | `eve_trader/refining/models.py` | The whole file, ported in full rather than split - same reasoning as `constants.py`'s row above. Only `OreCandidate`/`OreShortlistItem`/`OreOption`/`MineralOption` have a consumer in this port's tests; `OreShortlistRow` and the Mineral Shopping List shapes (`MineralRequirement`, `OrePurchase`, `DirectMineralPurchase`, `MineralCoverage`, `ShoppingListPlan`) come along for the same "don't re-derive the split later" reason and wait for the modules that will actually build them. | Ported 2026-09-03, verbatim. |
| `eve_trader_local/refining/paste_parser.py` | `eve_trader/refining/paste_parser.py` | The whole file, verbatim: `parse_paste`'s 9-column tab-separated grammar (confirmed against evepraisal.com's own open-source `evepaste` reference implementation, per the module's own docstring), its short-split/trailing-empty-column padding, the quantity/volume cleaning regexes, and `merge_duplicate_stacks`' case-insensitive quantity summing. | Ported 2026-09-03, verbatim - genuinely pure, storage-free text parsing confirmed by reading it (no SDE lookups at all; `resolve_type_id`-style name resolution belongs to the not-yet-ported `reprocessing.py` quote module, one step downstream - see the "File-mapping note" in the reprocessing.py row below). Tests: `tests/test_refining_paste_parser.py` (all 9 columns, blank-line skipping, short-trailing-column padding, the malformed-line/unreadable-name-or-quantity error paths, thousands-separator/`m3`-suffix cleaning, and `merge_duplicate_stacks`' case-insensitive sum/first-seen-casing/error-dropping/order-preservation behavior). |
| `eve_trader_local/refining/reprocessing.py`, `eve_trader_local/refining/config.py` | `eve_trader/refining/engine.py` | The actual reprocessing-yield math: `ore_ice_base_yield`/`ore_ice_yield` (the real Upwell structure formula - rig points added to a 50-point base, then security and structure modifiers applied multiplicatively to that whole sum, then the Reprocessing/Reprocessing Efficiency/ore-family skills and implant bonus layered on top), `scrapmetal_yield` (the deliberately independent non-ore/ice path - structure/rig/security/implant/the two ore-path skills have no effect there), and `apply_reprocessing_yield`'s two real-EVE roundings (whole-portion batching via `storage.get_portion_size` before any yield is applied, then each output material floored independently rather than the total). **File-mapping note, not a divergence**: the parent's `refining/reprocessing.py` is actually the *Reprocessing-tab quote calculation* (issue #92 - wraps this same yield math with live ESI/Goonmetrics pricing and a buy-vs-sell recommendation), while the yield math itself lives in the parent's `refining/engine.py`. This repo ports the yield math now (it's pure and has no pricing dependency) and names the file for what's actually in it rather than mirroring the parent's file-name split one commit early; `reprocessing.py`'s own module docstring documents this explicitly so a future diff against the parent doesn't get confused by the name collision. Added here to support it: `storage.get_portion_size`/`storage.get_type_materials` (both pure reads over `sde_types`/`sde_type_materials`, tables `sde.py` already populates; the parent's `lru_cache` dropped, same call this repo made for every other SDE read) and a new `RefiningConfig` (`refining/config.py`, scope key `"refining"`, loaded/validated exactly like `ProductionConfig`/`DoctrineConfig` - structure_type/rig_tier/implant enum-checked against `constants.py`'s option dicts, skill levels range-checked 0-5 in the shared `config.py`) carrying only the ore/ice+scrapmetal yield fields the ported math reads; `refining_tax_rate` (the parent's own economics field) has no consumer yet at this point and is added in the `pricing.py`/`quote.py` row below, alongside the modules that actually read it. `cli.py`'s `main()`/`cmd_config` were extended to call `refining_config.reload()` alongside the existing Trading/Production/Doctrine calls, closing the same "a saved Settings override never actually took effect on the next CLI invocation" gap Doctrine's own port fixed for itself. Tests: `tests/test_refining_reprocessing.py` (the confirmed 90.63%/55% ceilings from constants.py's own docstring, the unknown-ore-family-defaults-to-maxed rule, the scrapmetal path's confirmed independence from every ore/ice-path input, whole-portion batching including the below-one-portion case, and per-material independent flooring) and `tests/test_refining_models.py` (mutable-default-dict safety for `OreOption.yield_per_portion`, `landed_cost_per_portion`). |
| `eve_trader_local/refining/pricing.py` | `eve_trader/refining/pricing.py` | The Ore Shortlist's per-row profit calculation (issue #91): `landed_cost_per_unit` (Jita sell percentile x (1 + broker fee) + haul cost - the same formula `production/pricing.py`'s own `_candidate_prices` uses for its Jita leg, independently re-derived here since the parent keeps the two tools' pricing modules separate), `mineral_type_ids_for`'s per-candidate-list material union, `_decision`'s Inactive → No market data → Skip/Import precedence (no "Already ordered" tier - the Ore Shortlist has no own-orders tracking, confirmed out of scope), and `evaluate_ore_item`/`evaluate_ore_shortlist`'s portion-size-normalized landed-cost/mineral-value/net-sell/profit/margin math, including the confirmed real bug this file's own comment documents (the profit-vs-`min_profit_threshold` comparison must use the per-*unit* figure, not per-portion, or the threshold is ~portion_size times too lenient). | Ported 2026-09-03, near-verbatim. Takes `list[OreCandidate]` as a plain parameter throughout - it had no dependency on `candidate_discovery.py` (ported later, see the row below) at all, so this ported cleanly ahead of its own real caller, same precedent `shortlist.py`/`history_backtest.py` set earlier in this repo's Trading port. Uses this repo's own `.reprocessing` (the yield-math module, see the row above) where the parent imports its `.engine` - the only import-path adaptation needed. `RefiningConfig.refining_tax_rate` (deferred in the row above) is added here, since this is the first module that actually reads it. Tests: `tests/test_refining_pricing.py` (the parent's own suite carried over verbatim, plus one added case regression-testing this repo's "an unpriceable mineral is never treated as 0 ISK" principle explicitly, since the parent's suite only exercised that path incidentally through the no-market-data cases already covered). |
| `eve_trader_local/refining/candidate_discovery.py` | `eve_trader/refining/candidate_discovery.py` | The whole 56-line module: `_family_and_is_ice`'s ore-vs-ice family derivation (ore families come from the SDE group name itself - one "Compressed <Family>" group per family - while every compressed ice variant shares one "Compressed Ice" group, so ice needs its family parsed from the *type* name instead) and `build_ore_candidate_universe`'s straight SDE-row-to-`OreCandidate` mapping. | Ported 2026-09-03, verbatim. `pricing.py`'s `evaluate_ore_item`/`evaluate_ore_shortlist` already took `list[OreCandidate]` as a plain parameter (see the row above), so this ported cleanly as the last piece feeding it. Added here to support it: `storage.load_ore_ice_candidate_types` (ported from the parent's `storage.py` - the `t.type_name LIKE 'Compressed%'` filter, not `group_name`, is load-bearing per the parent's own comment: a compressed ore/ice type shares its *raw* ore's group in the real SDE, so filtering on group_name instead silently produces an empty universe). Genuinely just a pure SDE scan (like Trading's `build-universe`), with no live pricing dependency of its own, so it has no standalone CLI command - its only real consumer is the Ore Shortlist refresh action (`add-ore-to-shortlist`/`refresh-ore-shortlist`, see the `actions.py` row below), which is where it's actually exercised from the CLI now. Tests: `tests/test_refining_candidate_discovery.py`, against a synthetic SDE cache (`storage.replace_sde_data`) covering ore-family/ice-family derivation, real ore+ice discovery, and exclusion of unpublished/raw/non-ore-ice types. |
| `eve_trader_local/refining/quote.py` | `eve_trader/refining/reprocessing.py` (the real one - see the file-mapping note two rows up) | The Reprocessing-tab quote calculation (issue #92): `resolve_type_id`'s exact-match-only SDE name resolution (deliberately not fuzzy - a paste line's name should already be a real EVE item name), `mineral_type_ids_for_lines`, and `evaluate_reprocessing_line`'s sell-as-is-vs-refined-value comparison including the confirmed real bug its own module docstring documents (both options end in a C-J sell order, so both must incur `structure_sell_haircut` - the pre-fix version priced sell-as-is as a bare gross figure, systematically biasing borderline items toward "Sell instead"). Plus the `ReprocessingQuoteRow` dataclass. | Ported 2026-09-03, near-verbatim. **Naming decision (three refining-related files now exist across the two repos, so this is spelled out at length in `quote.py`'s own module docstring too, not just here)**: the parent's `refining/reprocessing.py` is this quote calculation; this repo's *own* `refining/reprocessing.py` (see two rows up) already ports the parent's *different* `refining/engine.py` (the pure yield math). Naming this new module `reprocessing.py` too would collide with a file that already exists here under that name and means something else. Named `quote.py` instead - it is the live-pricing wrapper around the already-ported yield math, matching the parent's behavior one-for-one, just not its filename. Uses this repo's own `.reprocessing` module for `apply_reprocessing_yield`/`scrapmetal_yield` where the parent imports its `.engine` - the only import-path adaptation needed beyond the module rename. Tests: `tests/test_refining_quote.py` (the parent's own suite carried over verbatim, plus one added case for the same "unpriceable material is never 0 ISK" principle `pricing.py`'s test row above adds). **Now wired to a CLI command** (`quote-reprocessing <path>`, see the `actions.py` row below): unlike `parse-fitting`'s pure-local smoke test, this needed a live seller-character ESI call (or the Goonmetrics fallback) for both the item's own price and its mineral yield's price - the same shape `do_quote_reprocessing` has in the parent's `actions.py` - so it wasn't inlined ahead of `actions.py` the way `parse-fitting` could be (that command needed only the already-ported SDE-backed parser, no network/auth at all). |
| `eve_trader_local/refining/optimizer.py` | `eve_trader/refining/optimizer.py` | The whole Mineral Shopping List linear program (issue #93): `optimize_shopping_list`'s LP formulation (one column per candidate ore = whole portions to buy, plus one per directly-buyable mineral = units to buy outright; objective = total landed ISK; one `>=` row per required mineral, `sum_i portions_i x yield_ij + direct_j >= required_j`, negated because `linprog` only speaks `<=`; all variables bounded at `>= 0`), the three modelling decisions its module docstring spells out (refining tax modelled as reduced yield rather than an ISK fee, surplus minerals given no credit, and - since the fix below - portion/direct quantities solved as real integers via `linprog`'s `integrality` param rather than relaxed-and-rounded), and `_repair_shortfalls` (a defensive backstop topping up any mineral with no direct-buy price whose floored yields left it short) plus the final under-coverage assertion. | Ported 2026-09-03, verbatim apart from the error type: the parent's own `OptimizationError` (which its `actions.py` converts) is dropped in favour of raising `ActionError` directly, since this repo keeps that type in `errors.py` precisely so any module can raise it without an import cycle. **Adds `scipy`/`numpy` to this repo's dependencies** - see the dependency note above. **2026-09-03: ported the parent's real-MIP fix (commit `90c669b`)**, closing the relax-then-round optimality gap noted here right after the initial port - the old code solved a continuous LP relaxation, rounded ore portions UP, then ran a post-hoc `_trim_rounded_plan` heuristic that could only ever *drop* a portion the relaxation had already picked, never *add* one it left at zero, even when that was the true cheapest whole-unit answer (confirmed via brute-force comparison: ~8% of small random cases over the true optimum, worst case 64% over - still always feasible, never under-covering). Fixed the same way as the parent: `linprog(..., integrality=...)` now marks BOTH the ore-portion and the direct-mineral-purchase columns as integer (direct-mineral needs it too - leaving it continuous re-opens a smaller version of the same gap, see the module docstring's decision 1) and solves that as the real plan; the old unconstrained continuous solve is kept once, only to report `lp_cost` as a theoretical floor. `_trim_rounded_plan` and its only caller-helper `_plan_cost` are now dead code and were removed; `_repair_shortfalls` is kept as a defensive backstop against solver float noise or a MIP time-limit fallback, plus the new `_MIP_TIME_LIMIT_SECONDS` (5s) safety net for pathological branch-and-bound cases. Both repos now have real integer programming here, not relax-then-round. Tests: `tests/test_refining_optimizer.py` (hand-checked optima for ore-wins/direct-wins/genuine-mix/overlapping-yield cases, the case a per-mineral greedy ranking would lose, every failure path, a randomised property suite asserting no requirement is ever under-covered and costs always reconcile, and - replacing the old weaker brute-force spot-check - `test_matches_brute_force_optimum_on_random_small_cases`/`test_realistic_scale_solves_quickly`, ported from the parent's own new regression tests: the former independently brute-force-enumerates every whole-unit combination across 200 random small cases and asserts the solver matches the true optimum, not just feasibility; the latter is a solve-time sanity check at the tool's realistic scale). |
| `eve_trader_local/refining/actions.py` | `eve_trader/refining/actions.py` | The Ore-&-Minerals-relevant `do_*` subset: `do_add_ore_to_shortlist`/`do_refresh_ore_shortlist`/`do_deactivate_ore_shortlist_items`/`do_activate_ore_shortlist_items`, `do_quote_reprocessing`, `do_list_refinable_minerals`/`do_load_mineral_requirements`/`do_save_mineral_requirements`/`do_optimize_mineral_shopping_list`, and `do_update_settings`. Same thin-orchestration/`ActionError`-boundary shape as `actions.py`/`production/actions.py`/`doctrine/actions.py` - reuses Trading's own seller character/C-J structure throughout, same as the parent, since there's no Ore-specific login. | Ported 2026-09-03. Deliberately different from the parent in one place, confirmed by reading its actual call chain rather than assumed: the parent's `do_optimize_mineral_shopping_list` sources its mineral requirements from a saved, tenant-scoped `mineral_requirements` table (edited by a web UI, or defaulted from it) - not from a Production build's material list, which the module docstring's own reasoning ("the minerals are consumed by Production") could suggest but the real code never wires up. This repo ports that same saved-list default (a new `storage.mineral_requirements` table) rather than inventing a live Production-BOM link the parent itself doesn't have. `do_get_ore_shortlist` is a small addition with no parent counterpart - the parent's web frontend reads `storage.latest_ore_snapshot()` directly, but this repo's CLI needs a `do_*` to print something after a refresh, same precedent as Production's `do_list_stock_targets`. `do_set_mineral_requirement`/`do_remove_mineral_requirement` are also new: the parent's `do_save_mineral_requirements` always replaces the whole list (a web editor submits it wholesale), which doesn't fit a one-item-at-a-time CLI command the way `set-stock-target` needs to - both load the existing saved list, upsert/remove one entry, and re-save through that same replace-all path, so there is still only one write path into the table. `optimizer.optimize_shopping_list` already raises `ActionError` directly here (see that module's own port note), so no `OptimizationError` catch-and-convert step exists the way the parent's does. Added here to support it: the `ore_shortlist`/`ore_shortlist_snapshot`/`mineral_requirements` SQLite tables (`storage.py`, no `tenant_id` - same two-table live-list/append-snapshot shape as Trading's own `shortlist`/`shortlist_snapshot`) with `upsert_ore_shortlist`/`load_ore_shortlist`/`deactivate_ore_shortlist_items`/`activate_ore_shortlist_items`/`save_ore_shortlist_snapshot`/`latest_ore_shortlist_snapshot` and `replace_mineral_requirements`/`load_mineral_requirements` - raw tuples in/out, not `refining.models` dataclasses, matching the parent's own "`storage.py` never imports a submodule's own models" precedent. CLI: `add-ore-to-shortlist`, `refresh-ore-shortlist`, `list-ore-shortlist`, `quote-reprocessing <path>`, `set-mineral-requirement <item> <qty>`/`remove-mineral-requirement <item>`/`list-mineral-requirements`, `solve-shopping-list` - one command per action, printing only. Tests: `tests/test_refining_actions.py`, following `tests/test_actions.py`'s own no-network stub pattern (`refining.actions.ESIClient`/`.TokenManager`/`.GoonmetricsClient` monkeypatched) against a real synthetic SDE cache for the yield/portion-size math - covers each `do_*`'s happy path and failure modes (empty SDE cache, empty shortlist, a failed structure order-book fetch, an unresolvable paste line, duplicate/unknown/non-positive mineral requirements, no requirements to solve for) plus one end-to-end-ish test solving a real small LP (discover the ore universe, price it against mocked Jita/structure stats, confirm the ore route undercuts buying the mineral outright). |

### Station Trading

A separate tool from Trading/Production/Doctrine/Ore & Minerals in the parent
repo too - the five share only the SDE/ESI/config foundation. Real mechanic,
confirmed by reading `actions.py`'s module docstring and
`candidate_discovery.py`/`undercut.py` rather than assumed from the name
alone: unlike the other four tools' Jita-import-to-C-J-sell (or
buy-ore-at-Jita-refine-and-sell-at-C-J) model, Station Trading buys and sells
*within the same station* - Jita's own NPC trade hub - profiting from the
bid-ask spread itself (classic EVE "station trading"/market-making), never
touching the C-J structure at all. Fully ported now: the trade-skill
order-slot constants, `StationTradingConfig`, the Goonmetrics-driven
candidate scan plus its bounded live-ESI shortlist confirmation, the
bidirectional own-order undercut/outbid check, trader-character
registration, and the orchestration layer (`actions.py`) wiring all of it
together against real ESI/Goonmetrics prices and CLI commands.

| File here | File in eve-trader | What's shared | Status |
|---|---|---|---|
| `eve_trader_local/station_trading/constants.py` | `eve_trader/station_trading/constants.py` | The whole file, verbatim: the seven real trade-skill type_ids (confirmed against the parent's own live SDE cache, not trusted from memory - the parent's own docstring documents catching a wrong remembered id for Tycoon and a since-removed "Margin Trading" skill this way), `SKILL_LABELS`, and `order_slots_from_skills`' base-5-plus-per-skill-per-level order-slot formula. | Ported 2026-09-03, verbatim apart from a docstring note confirming no real overlap with `production/constants.py`'s own `job_slots_from_skills` (a different skill tree entirely - industry job slots, not market order slots) - checked directly rather than assumed, since the two are shaped similarly enough to invite confusing them. |
| `eve_trader_local/station_trading/config.py` | `eve_trader/station_trading/config.py` | The `StationTradingConfig` field definitions and their defaults/reasoning in full: the real Jita trade-hub station_id, the itemized broker-fee/sales-tax rates (charged differently per leg - see the field's own comment), and the discovery gates (`min_spread_threshold`/`min_daily_volume`, both checked live against the real Jita market in the parent repo - the docstring's own account of why 1000 sits in the real gap between dead and liquid items came over verbatim) plus the same off-by-default shortlist-cap shape `TradingConfig` already has. | Ported 2026-09-03. Follows this repo's now-three-times-established pattern (`ProductionConfig`/`DoctrineConfig`/`RefiningConfig`) rather than the parent's `ConfigProxy`/contextvars machinery: no enum-style fields exist here (unlike Production's structure/rig checks), so no validation layer sits on top of the shared `validate_config_overrides` - same "nothing extra needed yet" shape `doctrine/config.py` already has. Storage scope key is `"station_trading"`. Added to the shared `config.py`'s `_FIELD_RANGES` (keyed by field name, not class, same precedent every prior tool's config row used): `station_id` (`(1, None)`), `broker_fee_rate`/`sales_tax_rate`/`min_spread_threshold` (`(0, 1)` - real fractions), `min_daily_volume` (`(0, None)`); `max_active_shortlist_items` already existed for `TradingConfig` and is reused as-is, since range-checking is keyed by field name. |
| `eve_trader_local/station_trading/candidate_discovery.py` | `eve_trader/station_trading/candidate_discovery.py` | The whole two-stage discovery shape: `discover_candidates`' single Goonmetrics current-price dump for the entire Jita market, ranked by bid-ask spread gated on `cfg.min_spread_threshold`, then real average daily traded volume (Goonmetrics region history for `TRADING_CONFIG.jita_region_id` - never order-book depth, same CLAUDE.md "Theoretical ceiling figures" reasoning every other tool's discovery already follows) gated on `cfg.min_daily_volume` and only fetched for items that already passed the spread filter - and `confirm_live`'s bounded `region_order_stats_bulk` call against an already-narrowed, persisted shortlist, never the whole market (no bulk-region endpoint exists to price "everything Goonmetrics knows about Jita" any other way). | Ported 2026-09-03, verbatim. Both `GoonmetricsClient`/`ESIClient` in this file's real (non-test) callers are the already-ported classes with identical method signatures (`current_prices`, `price_history_chunked`, `region_order_stats_bulk`), so the only change from the parent is the import path. `top_n`'s "off unless explicitly passed or `cfg.enforce_shortlist_cap`" behavior came over intact - same "the real noise filter is `min_daily_volume`, a cap on top of that is an explicit user choice" reasoning `TradingConfig.enforce_shortlist_cap` already documents. |
| `eve_trader_local/station_trading/undercut.py` | `eve_trader/station_trading/undercut.py` | The whole file, verbatim: `check_undercut_pooled`/`check_buy_undercut_pooled`'s bidirectional own-order-vs-competitor-order comparison at `cfg.station_id`, pooled across every registered trader character (a cheaper/higher order from one of *your own* other characters must never read as an undercut/outbid - the same reasoning this repo's own `own_orders.py` already carries from the parent's issue #46), and `_check_side`'s `order_id`-only cross-referencing (ESI's order-book schema has no owning-character field, so price/type_id matching would false-positive on a coincidentally identical competitor price - confirmed against the real ESI schema, same finding `own_orders.py`'s own port already recorded). | Ported 2026-09-03, verbatim apart from the docstring's cross-reference (naming this repo's own `own_orders.check_undercut_pooled`, which matches the parent's function name exactly, rather than re-deriving the comparison). No `ESIClient` changes needed: `character_orders`/`region_orders_raw` are both already-ported methods with identical signatures. |
| `eve_trader_local/station_trading/esi_sync.py` | `eve_trader/station_trading/esi_sync.py` | Character registration for the `trader` role: `STATION_TRADING_ROLE_PREFIX`, `STATION_TRADING_SCOPES` and `list_trader_characters`. Confirmed by reading the real code rather than assumed: Station Trading does **not** reuse Trading's own `buyer`/`seller` characters - it registers its own distinct `trader` role prefix and scope pair (`esi-markets.read_character_orders.v1` + `esi-skills.read_skills.v1`), separate from `TradingConfig`'s own OAuth scopes, even though it operates in the same station/structure sense that Production's `producer` role and Trading's `buyer`/`seller` roles are already kept separate from each other. There is deliberately no `sync_esi()`-style bulk pull here at all (unlike Production's `esi_sync.py`) - own orders and skill levels are both read live, on demand, by `actions.py`, since an undercut check is only ever meaningful against the current live order book and a skill level changes too rarely to be worth caching ahead of time. | Ported 2026-09-03, verbatim apart from the import path (`list_records`, not the parent's `list_roles`, matching this repo's own `TokenManager` API - the same adaptation `production/esi_sync.py`'s own `list_producer_characters` already made). |
| `eve_trader_local/station_trading/actions.py` | `eve_trader/station_trading/actions.py` | The whole `do_*` set: `do_list_trader_characters`/`do_remove_trader_character`, `do_refresh_shortlist`/`do_get_shortlist` (sharing `_build_shortlist_rows`, which live-confirms every persisted row's price in one bounded `confirm_live` call rather than per-row) plus `do_deactivate_shortlist_items`/`do_activate_shortlist_items`, `do_check_undercut` (bidirectional, converting an `ESIError` to `ActionError`), `do_get_skill_summary` (live per-character trade-skill pull plus the derived order-slot count - informational only, per `constants.py`'s own "fee/tax discounts are never derived from these" reasoning), and `do_update_settings`. Plus `_profit`'s real EVE market-mechanic formula: broker fee charged on *both* legs (buy placement and sell placement) but sales tax only on the sell leg, and `profit_per_day = profit_per_unit * avg_daily_volume` - the same whole-market theoretical-ceiling convention (never a personal-achievable estimate, see CLAUDE.md) every other tool's own daily-profit figure already follows, safe from that section's documented order-book-depth bug because `avg_daily_volume` here is already real Goonmetrics region turnover, not `sell_volume`. | Ported 2026-09-03. `ActionError` comes from this repo's own `errors.py` (not `from ..actions import ActionError` the parent's real code path uses - this repo's shared error type already lives outside any single tool's `actions.py`, the same import path every other tool's `actions.py` already uses). `do_update_settings` returns `{"updated": list(updates.keys())}` rather than the parent's bare `updates` dict, matching this repo's own `doctrine`/`refining` `do_update_settings` convention rather than the parent's. Added here to support it: the `station_trading_shortlist` SQLite table (`storage.py`, no `tenant_id`, no append-only snapshot counterpart unlike Trading/Ore & Minerals' shortlists - nothing here needs a "what did this look like on an earlier run" history, since every row is re-priced live off the current order book on every read) with `upsert_station_trading_shortlist`/`load_station_trading_shortlist`/`deactivate_station_trading_shortlist_items`/`activate_station_trading_shortlist_items` - the upsert deliberately never touches `active` on conflict, so a re-discovery run can't silently reactivate a type_id the user explicitly deactivated (matches `do_refresh_shortlist`'s own documented contract, ported unchanged from the parent). CLI: `auth --role trader`, `refresh-station-shortlist`, `list-station-shortlist`, `check-station-undercut`, `station-trading-skills` - one command per action, printing only (no separate activate/deactivate commands yet, same "kept for a future GUI, no CLI command needed yet" precedent `do_list_doctrine_characters` already set). Tests: `tests/test_station_trading_constants.py`, `tests/test_station_trading_config.py`, `tests/test_station_trading_candidate_discovery.py`, `tests/test_station_trading_undercut.py`, `tests/test_station_trading_esi_sync.py`, `tests/test_station_trading_actions.py` - no network anywhere (`ESIClient`/`TokenManager`/`GoonmetricsClient` monkeypatched at the module boundary, or passed in directly where the ported functions already take a client parameter), against a real synthetic SDE cache for item name/category resolution. |

## The orchestration layer (`actions.py`) — an integration, not a port

`eve_trader_local/actions.py` (2026-09-02) is a different kind of entry from
every row above: the rows are file-for-file ports of business logic, this is
the *integration* that finally wires them together. It follows the parent's
`eve_trader/actions.py` structurally — same `do_*` names, same "thin
orchestration only, real logic stays in the other modules" rule, same
`ActionError` boundary the CLI catches — but it is a synthesis against this
repo's own modules, so a future diff against the parent's file will never
apply cleanly line-for-line. **Sync its *behavior*, not its text**: a change
to the parent's step ordering, pruning policy, failure isolation or a
`do_*`'s documented result shape is a sync candidate; its tenant/config/
DataFrame plumbing is not.

What's covered: `do_list_buyer_characters`/`do_list_seller_characters`/
`do_remove_trading_character`, `do_wallet_balance`/`do_wallet_transactions`,
`do_update_settings`, `do_build_universe`/`do_build_focused`,
`do_find_new_candidates`, `do_add_to_shortlist`, `do_refresh_shortlist`
(plus the shared `_refresh_shortlist_rows`/`_backfill_meta_levels`),
`do_shortlist_trends`, `do_check_seller_unlisted_stock`/`do_check_undercut`,
`do_refresh_and_prune_candidates` (with `_items_past_skip_grace_period`/
`_items_beyond_rank`/`_items_to_reactivate` and
`shortlist_skip_deactivation_days`), `do_reconcile_trades` and `do_pipeline`.
The parent's two real behavioral lessons came over in the code: `do_pipeline`
calls `do_refresh_and_prune_candidates`, never a separate find+refresh pair
(the parent's "Run Complete Pipeline" once added and pruned nothing at all
because of that), and each of its steps is isolated in its own try/except so
a missing login or a network hiccup in one can't stop the others.

Deliberately excluded: `do_create_backup`/`do_list_backups` (the parent's
`backup.py` shells out to `docker exec pg_dump` — see "Never sync"), and
anything tenant/admin-shaped. `do_auth` is excluded too: `cli.py`'s `auth`
command calls `TokenManager.login` directly, and this repo's TokenManager
has only ever stored characters under `f"{prefix}:{character_id}"`, so the
parent's legacy-single-key reconciliation in `_list_role_characters` has
nothing to reconcile here — it is one `tm.list_records(prefix)` call.
`do_recategorize_shortlist` and `do_list_transaction_characters` are left
out as web-UI-specific one-offs (a one-time fix-up for a bug this repo's
shortlist never had, and a character picker for a tab that doesn't exist);
port them if a GUI needs them. `do_wallet_transactions` returns raw
`location_id`s where the parent resolves display names — that needs the
parent's persisted `structure_names` table, which this repo has no
equivalent of yet.

Added here specifically to support this layer:
- `storage.py`: the `new_candidates`, `goonmetrics_history`,
  `candidate_search_cursor` and `shortlist_skip_streak` tables (the
  persistence `candidate_discovery.py`/`history_backtest.py` were
  deliberately ported without, since their caller owns it) with
  `save_new_candidates`/`latest_new_candidates`, `save_goonmetrics_history`/
  `read_goonmetrics_history_for_types`, `get_candidate_search_offset`/
  `set_candidate_search_offset`, `get_shortlist_skip_since`/
  `start_shortlist_skip_streak`/`clear_shortlist_skip_streak`, plus
  `sde_type_names`. `latest_new_candidates` returns `NewCandidateResult`
  objects and `read_goonmetrics_history_for_types` returns `HistoryPoint`
  objects where the parent returns DataFrames (no pandas here) — the latter
  imports `HistoryPoint` *inside* the function, since a module-level import
  would close a `storage -> goonmetrics_client -> config -> storage` cycle.
  `new_candidates` is append-only (a safe-mode run only ever covers a
  rotating window, so earlier runs are the rest of the picture, not stale
  duplicates); reads filter to `MAX(run_ts)` themselves.
- `config.py`: `TradingConfig.skip_grace_period_days`,
  `enforce_shortlist_cap` and `max_active_shortlist_items` — the pruning
  policy fields the `shortlist.py` row deferred to exactly this layer, with
  `_FIELD_RANGES` entries for the two numeric ones.
- `cli.py`: `build-universe`, `find-candidates` (`--safe/--full`, matching
  the parent's own flag pair), `add-to-shortlist`, `refresh-shortlist`,
  `check-unlisted-stock`, `check-undercut`, `reconcile-trades` and
  `pipeline` (`--rebuild-universe`). One command per action, printing only —
  no logic, so the future GUI calls the same `do_*` functions.

One deliberate behavioral difference from the parent, not an oversight:
`do_add_to_shortlist` raises only when *no* candidate search has ever run,
and returns `{"added": 0}` when the latest run simply recommended nothing.
The parent raises on an empty table only, which amounts to the same thing
there (its table accumulates across tenants' runs); here the distinction has
to be explicit so a normal "nothing worth adding this run" outcome doesn't
abort `do_refresh_and_prune_candidates` before it prunes.

## Candidates — port from eve-trader when a real local feature needs it

None of these exist in this repo yet. When one gets ported over, add it to the table above and keep it in sync
going forward. Each entry names *what part* of the parent file is
shareable — usually the computation, not the surrounding
storage/`TRADING_CONFIG` wiring:

| File in eve-trader | Shareable logic |
|---|---|
| `eve_trader/models.py` (the rest), `eve_trader/*/models.py` | Plain dataclasses — portable as-is where they don't reference `storage`/`TRADING_CONFIG` directly. `Candidate`, `NewCandidateResult`, `ShortlistItem`, `ShortlistRow`, `RealizedTrade`, `UnlistedStockRow`, `UndercutRow` and Production's `InventionResult` are ported; the rest belong to modules still listed here, so each arrives with its own |
| `TradingConfig`/`ProductionConfig` **field definitions** (`config.py`, `production/config.py`) | The behavioral fields (region ids, thresholds, economics) — not the `ConfigProxy`/contextvars machinery around them; this repo's `config.py` already carries the local equivalent of the OAuth-related subset, and `production/config.py` carries the pricing/structure subset (see the Production row above for exactly which fields are still deferred and what needs each) |

## Never sync (intentionally divergent, or not applicable)

- `eve_trader/storage.py`, `eve_trader/tenant_scope.py` — Postgres/RLS is
  the whole reason this repo exists separately, not as a fork.
- `eve_trader/access_gate.py`, `eve_trader/admin.py` — multi-tenant/
  cross-tenant concepts with no local-single-user equivalent.
- `eve_trader/error_log.py` — self-hosted error tracking for the web
  *frontend*: `do_report_error` exists to be reachable, unauthenticated, by
  any tenant's browser tab when a React component crashes, and
  `do_list_errors` is an Admin-tool-only cross-tenant view of everyone's
  reports. Checked against the real code (not assumed from the module
  docstring alone) before ruling it out: neither half has a local
  equivalent to attach to — there is no frontend here to crash and report
  from, and no multi-tenant Admin surface to view a log across users. A
  future native GUI crashing is a real, different concern (see
  `logging_setup.py`'s row above, which *is* ported in simplified form for
  exactly that "record what went wrong so it can be diagnosed later"
  purpose) — but that's an unhandled-exception-to-a-local-log-file problem,
  not this module's networked report-then-list-across-tenants shape; a
  local GUI's own crash handler should log to that file directly rather
  than reimplement `error_log.py`'s truncation/storage/Admin-viewer split
  for an audience of one person on their own machine.
- `eve_trader/api/*`, `eve-trader`'s `frontend/*` — this repo's target UI is
  a native GUI, not a web frontend/backend split.
- `eve_trader/scheduler.py` — per-tenant background-job iteration; a local
  "scheduler" here (if ever needed) would be a single-user timer, not worth
  sharing code with this.
- `eve_trader/backup.py` — shells out to `docker exec pg_dump`; meaningless
  without Postgres.
- `eve_trader/sqlite_migration.py` — a one-time Postgres *cutover* tool;
  conceptually the opposite direction from this repo's SQLite-native
  storage.
- Any tenant's or local user's own data/settings — this file tracks code,
  never data.

## Maintenance

Update this table (both copies — see the mirrored `SYNC.md` in eve-trader)
whenever:
- A "candidate" module actually gets ported here → move its row to
  "Already ported".
- A genuinely new, storage/tenant-agnostic business-logic module is added
  in eve-trader → add it to "Candidates".
- A module you'd expect to share turns out to be too entangled with
  storage/config to port cleanly → note it under "Never sync" with why, so
  nobody re-attempts the same porting effort later.
