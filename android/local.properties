# Committed for now, per explicit instruction (2026-09-04) - this repo is
# private, so the EVE SSO client id below isn't considered sensitive here.
# Revisit (move back to gitignored, per-machine) before this repo is ever
# made public or the app is distributed.

# Path to your Android SDK - Android Studio writes this automatically on
# first sync if it's missing. Deliberately NOT set here even though this
# file is now committed - it's a real machine-specific absolute path, and
# accepting Android Studio's auto-write into this file would leak that
# path into the repo on the next contributor's first sync. Leave commented
# out; if Android Studio adds an sdk.dir line here, remove it again before
# committing.
# sdk.dir=C\:\\Users\\<you>\\AppData\\Local\\Android\\Sdk

# Registered at https://developers.eveonline.com the same way the desktop
# build's .env.example describes, but with this exact callback URL instead
# of a localhost one:
#   eveauth-eve-trader-local://callback
# (see AndroidManifest.xml's intent-filter / data/auth/TokenManager.kt for
# why a custom URI scheme, not a loopback port, is this platform's
# equivalent of the desktop flow.)
EVE_SSO_CLIENT_ID=6699653592a549dcb7914141497a0aa9
