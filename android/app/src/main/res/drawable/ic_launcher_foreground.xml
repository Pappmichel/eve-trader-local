<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon foreground: a plain "E" monogram in the app's own accent
     cyan (ui/theme/Color.kt's AccentCyan), kept inside the adaptive-icon
     safe zone (the centered 66dp square within this 108dp canvas, since
     launchers crop the foreground to varying shapes). A deliberately
     generic placeholder mark, not designed artwork - see
     ic_launcher_background.xml's own comment for why, and android/README.md/
     ROADMAP.md's Android section for the decision to defer a real icon. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Vertical spine -->
    <path
        android:pathData="M30,24 h12 v60 h-12 z"
        android:fillColor="#2ECDF5" />
    <!-- Top bar -->
    <path
        android:pathData="M30,24 h48 v12 h-48 z"
        android:fillColor="#2ECDF5" />
    <!-- Middle bar (shorter) -->
    <path
        android:pathData="M30,48 h36 v12 h-36 z"
        android:fillColor="#2ECDF5" />
    <!-- Bottom bar -->
    <path
        android:pathData="M30,72 h48 v12 h-48 z"
        android:fillColor="#2ECDF5" />
</vector>
