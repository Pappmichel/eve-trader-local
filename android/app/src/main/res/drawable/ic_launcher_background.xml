<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon background: a flat fill in the app's own dark window
     color (see ui/theme/Color.kt's BgWindow / res/values/colors.xml's
     bg_window) - a generic placeholder, not real artwork, per the
     project's own "generic icon for now" decision (see android/README.md
     and ROADMAP.md's Android section). Swap for a designed icon later
     via Android Studio's Image Asset tool, which regenerates every
     density/shape variant from one source image. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#0A0E14" />
</vector>
